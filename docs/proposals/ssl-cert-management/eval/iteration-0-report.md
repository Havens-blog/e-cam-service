# Iteration 0 Report — Pre-Revision (Freeform Findings)

```yaml
iteration: 0
title: "Pre-Revision (Freeform Findings)"
source: eval/freeform-review.md
hit_rate: 1.0
rubric:
  (all dimensions): N/A
```

## Triage Summary

13 项风险/问题发现 + 9 条改进建议（建议与风险一一对应，已合并为同一攻击点）。分诊结果：接受 10 项、部分接受 3 项、跳过 0 项。

## ATTACK_POINTS

- **[high]** 变更清单生成与引用扫描新鲜度未绑定，陈旧扫描数据导致批量更换漏报 | quote: "漏报叠加陈旧扫描，等于把提案声称要消灭的'漏换事故'换了个发生位置。" | improvement: 引入覆盖率/新鲜度元数据，变更清单生成强制绑定最近扫描，超期扫描阻断清单生成
- **[high]** 变更清单以同域名为聚合键，与提案自身"同域名多证书并存需区分"的边界场景矛盾 | quote: "聚合键选错，批量执行会更换掉本不该动的引用。" | improvement: 聚合键改为同证书（指纹），显式建模通配符与多 SAN 证书
- **[high]** 回滚语义不完整：回滚范围、回滚目标有效性（旧证书可能已删除/已过期）未定义，"回滚成功率 100%"在这些场景不可达成 | quote: "若旧证书在云证书库中已被删除或已过期，回滚目标根本不存在，'回滚成功率 100%'这个成功标准在这些场景下不可达成也不应达成。" | improvement: 补全回滚语义（范围、目标有效性前置检查、回滚自身失败路径），修正 SC 表述
- **[high]** 两段式上传第二段失败无补偿设计，云侧孤儿证书累积占用托管配额 | quote: "以阿里云为例，数字证书管理服务有托管配额，孤儿证书的累积不是卫生问题而是容量问题。" | improvement: 增加补偿事务设计与云侧证书生命周期管理（映射表 + 孤儿清理���
- **[high]** 建议将变更清单聚合键改为同证书指纹并显式建模通配符与多 SAN 证书（与第 2 条同源，已合并）
- **[high]** 建议补全回滚完整语义（与第 3 条同源，已合并）
- **[high]** 建议为两段式上传设计补偿事务与云侧证书生命周期管理（与第 4 条同源，已合并）
- **[high]** 建议引入覆盖率元数据并将变更清单生成与扫描新鲜度强制绑定（与第 1 条同源，已合并）
- **[medium]** 抽样一致率只度量查准不度量查全，SC-2 指标给运维虚假安全感 | quote: "人工核对通常只能验证'发现的项确实存在'，而批量更换决策需要的是'不存在的项确实存在'。" | improvement: SC-2 补充查全率度量方式或明确该指标仅表征查准
- **[medium]** VM Nginx 引用盲区未在引用视图中显式声明，"全部引用"措辞误导运维 | quote: "若 UI 不显式标注'此视图不含 VM Nginx 配置级引用'，运维做完批量更换、验证全部成功，仍可能漏掉 Nginx 上的旧证书" | improvement: 引用视图与变更清单增加覆盖率边界声明（首期不含 VM Nginx 配置级引用）
- **[medium]** 缺"平台证书 ↔ 各云证书 ID"映射表，回滚与重复上传去重缺基础数据 | quote: "提案未提及此映射，而这正是回滚和重复上传去重的基础数据。" | improvement: In Scope 增加映射表设计项
- **[medium]** 部分成功后的域名分裂状态与 TLS 常规探测告警未区分，告警噪声致监控失效 | quote: "误报刷屏会让真正的差异告警被忽略，这正是监控体系失效的标准路径。" | improvement: 定义变更后验证阶段与常规探测的一致性规则，建模部分成功预期状态
- **[medium]** K8s CRD patch 可能被 GitOps/控制器 reconcile 回写覆盖且无感知 | quote: "patch 前不确认资源管理权（是否 GitOps 管理、是否有控制器 own 该字段），这类失败无法靠重试解决。" | improvement: CRD 更新前增加资源管理权探测，变更后复检
- **[medium]**（部分接受）私钥安全仅"加密存储"一句话，解密使用环节（解析/校验/上传云证书库）与密钥管理体系未展开 | quote: "'加密存储'四个字背后的密钥管理体系才是攻击者的真正目标。" | improvement: proposal 层 NFR 补充私钥使用环节安全约束与密钥管理要求（详细设计留 PRD/tech-design，提案不展开选型）
- **[medium]** 证书删除操作零拦截，与导入"100% 拦截"不对称 | quote: "提案为导入设计了完整拦截，为删除设计的是零拦截，这个���对称没有理由。" | improvement: 删除增加引用检查与回滚保护（被引用证书不可删除或需强制确认）
- **[medium]**（部分接受）三云首期只发现不更换，"看得到却换不了"的运营风险窗口未声明 | quote: "否则分期切分把最高风险悄悄留在了首期之外。" | improvement: 在 Key Risks 增加该运营窗口风险与缓解（二期排期绑定引用点增长、监控告警兜底）
- **[medium]** 告警渠道依赖"待确认"却支撑可勾选的 SC-3，二者矛盾 | quote: "一个'待确认'的依赖支撑着一个可勾选的成功标准，二者不能同时成立。" | improvement: 将告警渠道确认列为 PRD 前置依赖，SC-3 改为"按已配置渠道触达"并明确渠道确认完成前 SC 不可验收
- **[low]** 建议定义变更后验证阶段与 TLS 探测一致性规则（与第 8 条同源，已合并）
- **[low]** 建议为证书删除增加引用与回滚保护（与第 11 条同源，已合并）
- **[low]** 建议度量查全率并给出三云能力缺口运营设计（与第 9/16 条同源，已合并）

## Classification Audit

| 发现 | 分诊 | 理由 |
|------|------|------|
| F1 扫描新鲜度 | 接受（结构性） | 可验证的内部矛盾：提案目标是消灭漏换，陈旧扫描使漏换复发 |
| F2 域名聚合键 | 接受（事实修正） | 与提案自身"同域名多证书并存"边界场景直接矛盾 |
| F3 回滚语义 | 接受（结构性） | SC-4 可验收性与回滚目标有效性矛盾，可验证 |
| F4 两段式补偿 | 接受（结构性） | 阿里云托管配额为已知现实约束，提案缺失补偿设计 |
| F5 查全率度量 | 接受（事实修正） | SC-2 指标定义可验证地不完整 |
| F6 Nginx 盲区声明 | 接受（事实修正） | 首期范围明确排除 Nginx 配置级引用但视图/UI 未声明 |
| F7 映射表缺失 | 接受（结构性） | 两段式设计的必要基础数据未列入范围 |
| F8 分裂状态告警 | 接受（结构性） | 部分成功场景与探测告警耦合，可验证缺失 |
| F9 CRD 管理权 | 接受（结构性） | GitOps reconcile 回写是可验证的技术现实 |
| F10 私钥生命周期 | 部分接受 | 诉求正当，但提案层只补 NFR 约束，选型与详细设计属 tech-design 职责 |
| F11 删除保护 | 接受（事实修正） | 与导入拦截的不对称可验证 |
| F12 三云运营窗口 | 部分接受 | 战略关切正当，提案层以风险条目呈现，运营设计细节留二期 |
| F13 告警渠道矛盾 | 接受（事实修正） | 依赖状态与 SC 可验收性直接矛盾 |

无 borderline 项，无 subjective-preference 跳过项。

## Edit Results (Reviser Output)

All 13 attack points addressed. Key edits:
- Solution 3：扫描时间/覆盖率元数据、指纹关联、Nginx 盲区声明（AP1/2/6）
- Solution 5：指纹聚合键、清单绑定最近扫描+超期阻断、完整回滚语义、变更后验证窗口（AP1/2/3/8）
- 删除拦截保护（AP11）；私钥使用环节 NFR 约束（AP10 部分接受口径）
- 告警渠道确认为 PRD 前置依赖（AP13）
- In Scope 新增云证书映射表项、部署器补偿与管理权探测（AP4/7/9）
- Key Risks 新增孤儿证书、三云运营窗口两条风险（AP4/12）
- SC 修正：查准/查全分离、回滚口径可验证化、K8s 复检（AP1/3/5/9/13）
- 分诊处置率 13/13 = 100%；接受+部分接受 = 13/13 = 100%
