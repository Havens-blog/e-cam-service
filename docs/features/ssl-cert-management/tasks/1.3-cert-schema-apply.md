---
id: "1.3"
title: "schema 落库验证（mongosh 执行 createCollection/$jsonSchema/createIndex 于开发库）"
priority: "P2"
estimated_time: "1h"
dependencies: ["1.2"]
type: "doc"
mainSession: false
---

# 1.3: schema 落库验证（mongosh 执行 createCollection/$jsonSchema/createIndex 于开发库）

## Description
`design/schema.sql` 已存在；本任务不编写代码，而是在开发库上用 mongosh 执行该 DDL，验证全部 createCollection/$jsonSchema 校验器与 createIndex 可成功执行、索引与唯一约束生效，并产出验证记录文档（含执行的命令、集合/索引清单、越界文档拒绝样例）。1.2 仓储层的 `EnsureIndexes` 与本任务的 DDL 应保持一致；若发现 schema.sql 与 1.2 实现存在偏差，记录差异并交回 1.2 修正。

## Reference Files
- docs/features/ssl-cert-management/design/tech-design.md — Data Models / 在途互斥的并发强制（activeMutex token）
- docs/features/ssl-cert-management/design/schema.sql: 待验证的 DDL 全文 (ref: Data Models)
- docs/features/ssl-cert-management/design/er-diagram.md: 索引策略对照表 (ref: Data Models)

## Affected Files

### Create
| File | Description |
|------|-------------|
| docs/features/ssl-cert-management/design/schema-apply-verification.md | 验证记录：执行的 mongosh 命令、集合清单、索引清单（含唯一/部分唯一/TTL）、越界文档拒绝样例、与 1.2 EnsureIndexes 的一致性核对结论 |

### Modify
| File | Changes |
|------|---------|
| docs/features/ssl-cert-management/design/schema.sql | 仅在验证发现偏差时补注注释/修正（无偏差则不改） |

### Delete
| File | Reason |
|------|--------|
| — | — |

## Acceptance Criteria
- [ ] 在开发库执行 schema.sql 全文：12 个 `createCollection` 均成功，`$jsonSchema` 校验器生效。
- [ ] 全部 `createIndex` 成功：唯一索引（uk_fingerprint/uk_fp_cloud_account/uk_cluster_group_kind）、部分唯一索引（uk_active_mutex partialFilterExpression activeMutex 存在；idx_status_heartbeat partialFilterExpression status=running）、TTL 索引（ttl_probe_90d/ttl_batch_session_30d）均存在且定义正确。
- [ ] 越界文档被拒绝：如 `hostingStatus: "bogus"`、`fingerprint: "not-hex"`、`status` 非枚举值等样例被 `$jsonSchema` 拒绝并记录。
- [ ] 部分唯一索引互斥验证：插入两条 `activeMutex` 相同的活跃单 → 第二条 duplicate key；终态 `$unset` 后可再插同 token。
- [ ] `schema-apply-verification.md` 产出，含执行命令、索引清单、与 1.2 `EnsureIndexes` 实现的一致性比对结论（一致 / 偏差列表）。

## Hard Rules
- 不在生产库执行；仅开发库；执行前确认连接目标。
- 不修改 schema.sql 的字段定义（仅在发现偏差时记录并交回 1.2，或仅补注注释）。

## Implementation Notes
- 执行环境：开发 MongoDB（连接串由本地 env 提供）。
- 用 `mongosh --file docs/features/ssl-cert-management/design/schema.sql` 逐段执行；分集合验证。
- 若 1.2 `EnsureIndexes` 与 schema.sql 存在不一致（索引名/字段/选项），记录差异清单，本任务不修复代码，回写 1.2。
- 验证记录文档面向后续运维（上线执行步骤参考）。
