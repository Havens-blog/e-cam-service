---
status: "completed"
started: "2026-08-18 19:58"
completed: "2026-08-18 20:19"
time_spent: "~21m"
---

# Task Record: 3.5 引用扫描服务（五云+K8s 快照扫描、coverageMeta 分母=asset 盘点固化、referenceStatus 三态、scan-timeout 恢复、防重）

## Summary
实现引用扫描服务：单次扫描创建 ScanSnapshot（running）→ 五云适配（CloudScanAdapter 只读端口注入）+ K8s 登记遍历（固定枚举+enabled 登记，certFieldPath 值抽取）发现 CertReference（指纹解析：映射反查→GetCert 要素→确定性占位指纹；snapshotId/scannedAt/namespace/kind 写通）→ coverageMeta 收敛（分母启动时从 internal/asset 聚合固化、不可用或 0-但历史非 0 → -1、covered>total 标记滞后）→ 终态（done/部分失败记入 partialFailures/全部失败 SCAN_DISCOVERY_FAILED）。防重（running → 409 SCAN_IN_PROGRESS）与 scan-timeout 恢复（SCAN_TIMED_OUT+告警事件+释放防重锁）。集成测试（mock ��云+fake K8s+内存仓储）22 例全绿。

## Changes

### Files Created
- internal/cert/service/reference_scan_service.go
- internal/cert/service/coverage_meta.go
- internal/cert/service/reference_scan_service_test.go

### Files Modified
- internal/cert/domain/cert_reference.go
- internal/cert/domain/scan_snapshot.go
- internal/cert/domain/repository.go
- internal/cert/domain/errors.go
- internal/cert/domain/models_test.go
- internal/cert/repository/scan_snapshot.go
- internal/cert/repository/cloud_cert_mapping.go
- internal/cert/repository/validators.go
- internal/cert/certtest/certtest.go
- docs/features/ssl-cert-management/design/schema.sql

### Key Decisions
- 无法解析指纹时写确定性占位指纹 sha256("certscan-unresolved:{cloud|account|certId}")：满足 cert_references 校验器 ^[0-9a-f]{64}$ 硬约束（schema required），保留引用并以 referencedCloudCertId 关联（AC 口径）；映射登记补齐后下轮扫描恢复精确关联
- K8s 引用 namespace/kind 落库为 schema 扩展：schema.sql/validators.go/models_test 三层同步演进（MongoDB 校验器允许附加属性；5.x 变更项重构 DeployTarget 依据）
- K8s coverageMeta 键为 (cloud="", product=crd)，total 恒 -1（asset 不盘点 K8s）；cloud 无 active 账号不入扫描范围（该云保持盲区声明，防空扫虚报 no_refs_scanned）
- 分母经 AssetCountSource 端口唯一入口（Hard Rule 防自指）：生产实现按 model_uid 候选表逐项 Count asset 实例（{provider}_{type} 约定，LB 家族后缀细分，首期启发式随 5.12 PoC 校准）
- 自定义 CRD GVR 解析：内置固定枚举精确命中；自定义登记按 plural=lower(kind)+s 与 v1/v1beta1/v1alpha1 候选探测，未命中计通道失败（discovery-only 只读约束全程不触发云侧写 API）
- 空范围（无账号且无登记）显式失败 SCAN_NO_CHANNELS 而非静默 done：空范围快照会使引用三态已扫描声明失真

## Test Results
- **Tests Executed**: Yes
- **Passed**: 22
- **Failed**: 0
- **Coverage**: 84.4%

## Acceptance Criteria
- [x] 扫描启动：创建 ScanSnapshot{startedAt,status=running}；已有 running 快照 → 409 SCAN_IN_PROGRESS（防重）
- [x] 五云+K8s 发现：逐云逐产品 ListReferences；K8s 按固定枚举+enabled 登记遍历、按 certFieldPath 读引用字段；CertReference 写 certFingerprint（映射/要素解析，无法解析保留引用以云证书 ID 关联）、snapshotId、scannedAt；K8s 引用含 clusterId/namespace/kind
- [x] coverageMeta 固化：扫描启动时按云×产品聚合 internal/asset 在用资源计数为 total；asset 不可用或计数异常（0 但历史非 0）→ total=-1；covered=本轮去重资源数；covered>total 以 covered 为准并标记滞后
- [x] 终态：全部成功 → done+finishedAt；某通道失败记入快照元数据（部分失败不阻塞其他云）；整体失败 → failed+failReason
- [x] scan-timeout 恢复 RecoverTimedOutScans()：running 且超 scanTimeoutHours → failed（SCAN_TIMED_OUT）+告警事件+释放防重锁（可重新触发）
- [x] 集成测试（mock 五云+fake K8s）：快照生命周期、coverageMeta 分母口径与 -1 分支、防重 409、超时恢复、K8s 引用字段（namespace/kind）落库

## Notes
验证：go build ./... / go vet ./internal/cert/... / gofmt 全部通过；go test ./internal/cert/... 全绿（domain/k8s/repository/service/web）；service 包覆盖率 84.4%（目标 80%）。仓储接口扩展 LatestRunning/ListRunningBefore/FinishScan 与 CloudCertMappingRepository.FindByCloudCertID（反查），certtest fake 同步实现。生产 shim（五云只读适配/K8s 网关/账号源/asset 计数器）随 7.1 调度装配接线；ScanAlertNotifier nil 时为 no-op。残留低覆盖仅为 shim 闭包（逻辑已抽取至 listCRDObjects/candidateGVRs/toDiscoveredRefs 覆盖）与真实 SDK 委托（3.1/3.2/3.3 各自包内测试）。
