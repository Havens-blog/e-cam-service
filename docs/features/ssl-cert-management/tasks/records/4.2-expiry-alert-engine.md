---
status: "completed"
started: "2026-08-18 20:50"
completed: "2026-08-18 21:54"
time_spent: "~1h 4m"
---

# Task Record: 4.2 到期分级告警引擎（30/14/7 分级计算、expiryAlertLevel 升级去重、四类告警事件发布）

## Summary
到期分级告警引擎：ComputeExpiryLevel 纯函数（daysLeft=ceil((notAfter-now)/24h)，thresholds 降序取最紧急命中级，默认[30,14,7]，expired 边界）+ InspectLedger 升级去重状态机（expiryAlertLevel 持久化，仅升级触发/同级不重发/换证出区间重置 none，发布先于落库 at-least-once，单证失败不中断整轮）+ CertAlertEvent 四类事件模型（expiry/tls_diff/change_linked/rollback_failed）与 CertAlertPublisher 统一发布接口（内存/日志默认实现，4.3 通道落地前默认路径）

## Changes

### Files Created
- internal/cert/service/inspection_service.go
- internal/cert/service/alert_events.go
- internal/cert/service/inspection_service_test.go

### Files Modified
- internal/cert/service/coverage_meta.go

### Key Decisions
- 档位标签按降序阈值序号映射（最缓档→L30、次档→L14、第3档起折叠L7），schema expiryLevels 允许 5 项而枚举仅 3 个非过期标签，单调折叠保升级去重不变式
- 升级路径先 PublishAlert 后 UpdateExpiryAlertLevel：发布失败不落状态，下轮巡检重发（at-least-once）；Hard Rule 去重状态持久化于文档字段而非内存（跨重启不重复告警有测试）
- thresholds 经 AlertConfigRepository.Get 读取单文档 _id=global，空/非法阈值回退 schema.sql DEFAULT [30,14,7]
- notAfter 零值证书跳过不���级，不误报 expired
- CertAlertEvent 不承载任何私钥字段，测试含 LEAK-MARKER 渗透式自查

## Test Results
- **Tests Executed**: Yes
- **Passed**: 24
- **Failed**: 0
- **Coverage**: 94.5%

## Acceptance Criteria
- [x] 分级计算：daysLeft=ceil((notAfter-now)/24h)；按 expiryLevels 降序匹配取最紧急命中级；已过期→expired
- [x] 去重状态机：升级触发并更新 expiryAlertLevel；同级/降级不重复；换证后重置 none 重新计级
- [x] CertAlertEvent 四类 category 统一模型 + PublishAlert(ctx,event) 接口 + 内存/日志默认实现
- [x] 到期分级事件按证书粒度聚合（指纹/SAN/级别/daysLeft/到期时间），不含私钥
- [x] 单元测试：30→14→7→expired 逐级升级、同级天级巡检不重发、换证重置后重新触发、levels 配置变化([45,20])正确计级

## Notes
验证：go build ./... OK；gofmt 干净（新建文件+误留空行修复）；go vet ./internal/cert/... OK；go test ./internal/cert/... 全绿（service/domain/k8s/repository/web）。新增 13 个测试函数（含 11 表驱动子测）。新文件函数级覆盖 94.5%（InspectLedger 错误分支/防御分支部分未覆盖，整体超 80% 目标）。Hard Rules 两条均满足且各有专项测试（持久化跨重启、category 字段承载四类）。调度注册与完整巡检流水线按任务边界留待 4.4。
