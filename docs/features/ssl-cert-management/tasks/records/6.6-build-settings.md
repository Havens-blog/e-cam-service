---
status: "completed"
started: "2026-08-19 04:38"
completed: "2026-08-19 04:49"
time_spent: "~11m"
---

# Task Record: 6.6 全局配置组件（UF-5：告警/豁免/CRD 登记/阈值界值）

## Summary
构建 /certs/settings 全局配置页（UF-5，跨仓库 e-cam-web）：告警接收卡（webhook mono 多行 + 邮件 tag 输入 + 发送测试 + 保存行内校验）、探测豁免清单卡（增删 Modal + 审计提示 + 即时刷新）、阈值参数卡（全部 10 项 thresholds 滑块/数字输入，界值与 schema.sql 1:1，越界 Error 边框 + 合法区间提示 + 保存禁用）、CRD 登记管理卡（登记/列表/删除回归盲区提示/重复 409 行内错误）；渠道未确认 Warning 横幅（channelConfirmed=false）；状态机含 loading 三卡骨架（800ms）、读取失败重试（表单不可编辑）、保存异常行内错误且草稿不清空、empty 尚未配置接收人 + 开始配置引导。阈值界值常量表 THRESHOLD_FIELDS 单一来源供渲染与校验共用；分卡保存经 buildUpdatePayload 透传未改字段（wildcardProbeOverrides/verifyWindowRoute 不丢）。角色拦截沿用 6.1 路由守卫，组件内不重复实现。

## Changes

### Files Created
- e-cam-web/src/views/cert/settings/format.ts
- e-cam-web/src/views/cert/settings/format.test.ts
- e-cam-web/src/views/cert/settings/components/AlertReceiverCard.vue
- e-cam-web/src/views/cert/settings/components/ExemptionListCard.vue
- e-cam-web/src/views/cert/settings/components/ThresholdsCard.vue
- e-cam-web/src/views/cert/settings/components/CrdRegistrationCard.vue

### Files Modified
- e-cam-web/src/views/cert/settings/index.vue

### Key Decisions
- SPEC CONTRADICTION：prototype settings.html 扫描新鲜度写 1~168，与任务 AC/schema.sql（1~72）冲突，按权威顺序取 AC+schema 1~72
- 「保护期 ≥ 验证窗口」由界值结构保证（verifyWindowHours 上限 24h < rollbackProtectDays 下限 7d），前端不做交叉校验仅保留提示文案
- webhookUrls ���端为数组 → mono 多行动态增删（原型单输入为数组长度 1 的特例）
- 保存异常保留输入：草稿状态由页面持有（受控组件），保存成功才以服务端返回回填；豁免增删后静默刷新 settings 不 clobber 未保存草稿
- PUT 越界 400 服务端信息在阈值卡保存按钮旁行内呈现（Hard Rule：前端界值仅为体验）
- CRD 卡自包含数据生命周期（独立端点组）；builtin 固定枚举登记不可删除（tooltip 说明）
- 重复登记 409 归类走 mapCrdCreateError（错误码/中文语义双通道识别），Modal 内行内呈现且表单保留

## Test Results
- **Tests Executed**: Yes
- **Passed**: 53
- **Failed**: 0
- **Coverage**: 0.0%

## Acceptance Criteria
- [x] 卡1 告警接收：webhook URL（mono）+ 邮件接收组（tag 输入）+ 发送测试（结果 Toast 成功/失败原因）+ 保存（URL/邮箱格式校验失败行内提示）
- [x] 卡2 豁免清单：表格（子域名/原因/操作人/时间/移除）+ 添加豁免 Modal（子域名+原因）；增删后列表即时更新；变更留审计提示
- [x] 卡3 阈值参数：全部 thresholds 以数字输入/滑块呈现，界值与后端 schema.sql 一致；越界 Error 边框 + 合法区间提示 + 保存禁用
- [x] CRD 登记管理：登记表单（clusterId+apiGroup+kind+certFieldPath）+ 列表（含 enabled）+ 删除（回归盲区提示）；重复登记 409 行内错误
- [x] 渠道未确认（channelConfirmed=false）顶部 Warning 横幅；就绪后消失
- [x] 状态：loading 三卡骨架 / 读取失败重试（表单不可编辑）/ 保存异常行内错误且保留输入不清空 / empty 尚未配置接收人

## Notes
跨仓库任务：代码位于 D:\Haven\e-cam-web（路径前缀 e-cam-web/），任务文件在 e-cam-service docs。验证：vitest run src/views/cert/settings/ → 1 file 53 tests passed；vue-tsc -b 零错误；eslint（--fix 后复检）零错误；npm run build:check 退出码 0（chunk 体积与 TagFilters 警告为存量页面既有）。coverage=null：e-cam-web 未安装 @vitest/coverage-v8（无 coverage provider，沿用 6.2-6.5 记录口径）。任务文件 just compile/fmt/lint 为 Go 仓库门禁，本任务未改 e-cam-service 代码故不适用，以上述前端等价门禁替代。代码未提交（按指示 Do NOT commit）。
