---
status: "completed"
started: "2026-08-18 20:39"
completed: "2026-08-18 20:49"
time_spent: "~10m"
---

# Task Record: 4.1 TLS 主动探测（SNI 拨测、通配符 wildcard_skipped+override、豁免、change_linked_diff 关联）

## Summary
Implemented TLS active probe service: ProbeService.ProbeDomains (single round, shared by daily + verify-window high-frequency probing in 5.10) and ProbeLedgerDomains (targets = all ledger Certificate.sans expanded+deduped, expectedDomain excluded). Per-domain SNI TLS handshake via crypto/tls (handshake-only, InsecureSkipVerify used solely to read peer leaf cert), onlineFingerprint=SHA256(leaf.Raw) + onlineNotAfter, six-state judgment (consistent/diff/change_linked_diff/unreachable/exempt/wildcard_skipped), wildcard SANs skipped by default with wildcardProbeOverrides concreteSubdomainOverride replacement, exemptions still probed but marked exempt, change_linked_diff associates verifying ChangeOrder via new repository method ListVerifyingActive. Hard Rules honored: probeDialTimeout=5s and probeConcurrency=8 constants (bounded per-domain timeout + batch concurrency), single-domain failure never aborts a round, no application-layer requests. Tests: local multi-cert SNI TLS server covering all six states, wildcard override, change-linked boundaries, bounded concurrency, handshake timeout; repo integration test for ListVerifyingActive (Mongo, auto-skip without DB).

## Changes

### Files Created
- internal/cert/service/probe_service.go
- internal/cert/service/probe_service_test.go

### Files Modified
- internal/cert/domain/repository.go
- internal/cert/repository/change_order.go
- internal/cert/repository/change_order_test.go

### Key Decisions
- Multi-cert same-domain ownership semantics fixed as 'online fingerprint matching ANY owning ledger cert = consistent' (Implementation Notes option) so cert-rotation coexistence periods do not raise false diff alerts; behavior pinned by TestProbe_MultiCertOwnershipSameDomain
- change_linked_diff required an additive ChangeOrderRepository.ListVerifyingActive(ctx, after) (status=verifying AND verifyWindowUntil>after, createdAt asc) — spec AC + tech-design win over the two-new-files note; additive, no existing implementer broken
- Wildcard with concreteSubdomainOverride: single result recorded on the override subdomain under normal judgment; no wildcard_skipped record for the covered SAN (avoids double-counting the dashboard wildcardSkippedCount gap)
- Exempt judgment applies only on successful dial; dial failure stays unreachable (truthful reachability, and unreachable never triggers diff alerts either)
- tlsDialer interface seam: production stdTLSDialer (real crypto/tls, SNI ServerName, deadline-bounded handshake) is directly exercised against the local SNI server in tests via addrOverride

## Test Results
- **Tests Executed**: Yes
- **Passed**: 13
- **Failed**: 0
- **Coverage**: 92.3%

## Acceptance Criteria
- [x] 探测目标域=台账全部 sans 展开去重（expectedDomain 不参与）；逐域名 tls.Dial 带 SNI 读 peer 证书 → onlineFingerprint+onlineNotAfter，写 ProbeResult{domain,probeAt,status}
- [x] 状态判定：指纹一致→consistent；不一致→diff；拨号失败/DNS 失败/超时→unreachable（不参与差异告警）
- [x] 通配符 SAN→wildcard_skipped 不拨测；wildcardProbeOverrides concreteSubdomainOverride→对子域名正常拨测、结果记于子域名按常规判定
- [x] 豁免清单命中域名→仍探测但 status=exempt（不触发常规差异告警）
- [x] change_linked_diff：diff 时查活跃验证中订单 verifyExpected，domain∈domains 且指纹一致→change_linked_diff+changeOrderId
- [x] 集成测试：本地多证书 SNI TLS server 覆盖六态+override+窗口关联；TTL 自然过期不主动清理

## Notes
Verification: go build ./... ok; gofmt clean on internal/cert; go vet ./internal/cert/... ok; go test ./internal/cert/... all ok (domain/k8s/repository/service/web). Coverage 92.3% = per-function average of new probe_service.go (weighted statement coverage of the new file); package-level service coverage is diluted by pre-existing files. 12 service tests + 1 repository integration test = 13. Per dispatcher instruction: NOT committed.
