---
status: "completed"
started: "2026-08-18 23:28"
completed: "2026-08-18 23:49"
time_spent: "~21m"
---

# Task Record: 5.3 ExecutionChannel 抽象与 CloudAPIChannel（接口定义、Credential/DeployTarget 载荷、两段式编排、模拟通道）

## Summary
ExecutionChannel 抽象（Discover/Deploy/Rollback/Type + ChannelType 四常量含 bastion/agent 预留）与 7 个 Service-Level Types（Credential/DiscoverScope/DeployTarget/DeployResult/RollbackResult/CloudCertInfo/BatchConf，逐项对齐 tech-design）；CloudAPIChannel 两段式编排（UploadCert→映射 active 先行→BindResource；第二段失败映射 active→orphan + CleanupOrphan 尽力补偿 + OrphanCandidate=true 供 5.9 队列消费）；SimulatedChannel（成功/失败/限流可配置）验证堡垒机/Agent 通道零上层改动可插拔性；BatchConf 校验与有效批大小 min(BatchSize,floor(total/2)) 供 5.7 复用；收敛 3.summary 遗留 SPEC CONTRADICTION——aliyun ErrCloudRateLimited 由包内定义改为再导出 cloudx 公共哨兵（对齐 tencent/huawei/aws/azure）；certtest 增补 FakeCloudCertMappingRepo；LedgerMaterialSource/SnapshotOldRefSource 生产来源实现（材料经 1.1 信封解密、旧引用经最新成功快照读取）

## Changes

### Files Created
- internal/cert/deployer/channel.go
- internal/cert/deployer/cloud_api_channel.go
- internal/cert/deployer/simulated_channel.go
- internal/cert/deployer/channel_test.go
- internal/cert/deployer/cloud_api_channel_test.go
- internal/cert/deployer/simulated_channel_test.go

### Files Modified
- internal/cert/certtest/change_fakes.go
- internal/shared/cloudx/aliyun/cert.go

### Key Decisions
- CloudDeployer 端口保持 tech-design Interface 2 五方法纯净形态；产品默认发现范围经 RegisterDeployer(cloud, deployer, products...) 装配期注册（5.4/5.5 组装 aliyun/tencent 实例），通道注册表按 target.Cloud 路由
- 映射写入时点=第一段成功后、第二段绑定前（active 先行）：AC-3 的 active→orphan 流转依赖文档先以 active 存在；崩溃恢复锚点（5.9 可据此发现未绑定产物）与 uk_fp_cloud_account 两段式去重同语义
- 补偿顺序=先映射 active→orphan（5.9 队列入口）再尽力 CleanupOrphan；补偿清理失败不吞错（主错误附加 compensation incomplete 提示）、映射保持 orphan 供 5.9 幂等重试（3.1/3.2 CleanupOrphan 对已删除幂等成功）
- 凭证明文零生命周期落地：Credential 按值传递副本在通道方法返回前 Zeroize（与调用方共享底层数组可观测清零）+ 防御性 String() 仅输出非敏感元数据 + 材料私钥 []byte 用毕 domain.Zeroize；错误链断言无私钥/凭证片段
- Rollback 仅重新绑定旧云证书 ID：GetCert 三判定（ROLLBACK_TARGET_INVALID）属 5.8 前置校验、被替换新证书孤儿标记归 5.8/5.9 编排（OrphanCleaned 由上层回填）——遵守'不在 deployer 层做业务级状态机判断' Hard Rule
- aliyun 哨兵收敛采用再导出（ErrCloudRateLimited = cloudx.ErrCloudRateLimited）而非删除：保持 3.1 调用形态零改动（assert.ErrorIs 恒等通过），仅消除包内第二哨兵定义
- EffectiveBatchSize 对 total<2 返回 total：单引用无灰度拆分意义，避免 5.7 产生空首批（floor(total/2)=0 退化），文档注明调用方应走单批全量或拒绝分批

## Test Results
- **Tests Executed**: Yes
- **Passed**: 27
- **Failed**: 0
- **Coverage**: 95.3%

## Acceptance Criteria
- [x] ExecutionChannel 接口定义 Discover/Deploy/Rollback/Type，ChannelType 常量 cloud_api/k8s_api/bastion/agent；Service-Level Types 字段+类型+约束与 tech-design 逐项对齐（Credential.Kind/Cloud/AccountKey/AccessKey/Secret/KeyVersion 等）
- [x] CloudAPIChannel.Deploy：两段式 UploadCert→BindResource；成功 → 写 CloudCertMapping{certFingerprint,cloud,accountKey,cloudCertId,status=active}；DeployResult{NewCloudCertID,OldCloudCertID,OrphanCandidate}
- [x] 第二段（BindResource）失败 → CleanupOrphan 补偿清理未绑定云侧孤儿证书 + OrphanCandidate=true（供 5.9 队列消费）；映射表状态流转 active→orphan
- [x] Credential 解密明文仅内存、用后 zeroing（复用 1.1），永不进日志/响应/审计；DeployTarget 字段按 channel 分支校验必填
- [x] SimulatedChannel 实现 ExecutionChannel（mock 成功/失败/限流可配置），用于 e2e 演示'堡垒机/Agent 通道零上层改动'可插拔性
- [x] 单元测试：mock CloudDeployer 覆盖两段式成功/第二段失败补偿/孤儿标记；SimulatedChannel 全路径

## Notes
验证：go build ./... 全绿；go vet ./... 全绿（golangci-lint 本机未安装，Makefile lint 目标自身跳过，同 Phase 1-3 先例）；gofmt 新文件全部 clean，change_fakes.go/aliyun/cert.go 仅剩既有 CRLF checkout 伪告警（LF 归一化 diff 验证内容 gofmt-clean，非本次引入）。go test -count=1 ./internal/cert/... 全绿；go test ./internal/shared/cloudx/... 全绿（哨兵收敛无回归）；deployer 包 27 顶层测试（含 60+ 子测试）覆盖率 95.3%。service 包首跑出现一次 TestInspection_RenewalResetThenRetrigger 时序抖动（隔离与全量 -count=1 复跑均绿，既有 flake 与本次改动无关）。限流退避有界重试属 5.4/5.5 部署器实现（本层仅 errors.Is 透传 cloudx.ErrCloudRateLimited）；bastion/agent 仅 Type 常量预留。生产装配（CloudDeployer 实例注册、材料/旧引用来源接线）随 5.4/5.5/7.1（同 3.5 生产 shim 顺延 7.1 先例）
