---
status: "completed"
started: "2026-08-19 04:16"
completed: "2026-08-19 04:38"
time_spent: "~22m"
---

# Task Record: 6.5 变更向导与管理组件（UF-4：7 步向导/阻断/分批/进度/验证窗口/报告详情/重入）

## Summary
Built UF-4 change management frontend (e-cam-web): list page with full status tab set (draft folded into 待确认, 已回滚/回滚失败 merged tab), 10s polling on executing/verifying rows with live per-row progress derivation, protection-period badge on old-cert side, supervisor-only 配置 entry, empty-state CTA; 7-step wizard (select certs with ?certId= preselect rules → auto-run precheck with code-mapped block cards (SCAN_STALE→立即扫描 / SAN / CHANGE_IN_FLIGHT / NEW_CERT_FINGERPRINT_ONLY) → list partition (executable + Warning-toned blocked section with reason/出路 + blindspot banner + snapshot age) → confirm with floor(total/2) first-batch cap and SCAN_STALE snapshot-recheck fallback to Step2 → 2s-polled execution progress with failed-item rollback banner → verify window with a11y countdown (hour-bucket announce, reduced-motion hides seconds), rollback + remaining-batch entries → terminal report entry); report detail page with 5 cards (list / per-item results with result+batch filters / rollback status / verify conclusion with unmet list + window summary / orphan cleanup), JSON export, read-only resume view (executing embeds Step5, verifying embeds Step6, poll auto-recovery with 2s→10s backoff; partial-in-protect rollback entry + badge; draft/pending 继续编辑), and manual remaining-batch confirm modal. All rollback entries gated to the three Hard-Rule states and scoped to success items only; no list-editing UI anywhere during execution.

## Changes

### Files Created
- e-cam-web/src/views/cert/changes/format.ts
- e-cam-web/src/views/cert/changes/format.test.ts
- e-cam-web/src/views/cert/changes/components/StatusTabs.vue
- e-cam-web/src/views/cert/changes/components/ChangeTable.vue
- e-cam-web/src/views/cert/changes/components/RollbackModal.vue
- e-cam-web/src/views/cert/changes/components/RemainingBatchModal.vue
- e-cam-web/src/views/cert/changes/new/index.vue
- e-cam-web/src/views/cert/changes/new/steps/Step1Select.vue
- e-cam-web/src/views/cert/changes/new/steps/Step2Precheck.vue
- e-cam-web/src/views/cert/changes/new/steps/Step3List.vue
- e-cam-web/src/views/cert/changes/new/steps/Step4Confirm.vue
- e-cam-web/src/views/cert/changes/new/steps/Step5Progress.vue
- e-cam-web/src/views/cert/changes/new/steps/Step6Verify.vue
- e-cam-web/src/views/cert/changes/new/steps/Step7Report.vue
- e-cam-web/src/views/cert/changes/detail/index.vue
- e-cam-web/src/views/cert/changes/detail/components/ReportCards.vue
- e-cam-web/src/views/cert/changes/detail/components/ResumeView.vue

### Files Modified
- e-cam-web/src/views/cert/changes/index.vue
- e-cam-web/src/router/routes.ts
- e-cam-web/src/utils/cert-permission.ts

### Key Decisions
- List progress + protectDaysLeft not present in backend list VO: derived live progress via getChangeProgressApi on active rows (10s tick, terminal rows backfilled once); protect badge renders from optional protectUntil when backend enriches it, report page uses ChangeDetail.protectUntil (documented in format.ts header)
- Confirm-time snapshot recheck failure maps to 409 SCAN_STALE (backend verifyReferenceConsistency): Step4 branch shows 引用清单已变化，请重新预检 and auto-falls back to Step2 re-precheck
- No draft-persist endpoint exists (POST /changes creates pending_confirm directly): 存为草稿 = sessionStorage wizard draft + already-generated order persisted server-side as 待确认, resumed via 继续编辑 (?orderId=); re-generating after changing selection cancels the wizard's own pending order first to release activeMutex
- No per-item verification endpoint: Step6/卡4 render exec-status proxy verdicts + verify summary + unmetDomains (adaptation documented)
- CHANGE_IN_FLIGHT error envelope carries no conflicting orderId: mutex block links to /certs/changes (active tabs) instead of a direct CHG-XXXX jump
- Batch-paused orders (executing+paused with remaining batches) stay on Step6 for the 执行剩余批 entry per transition table; ConfirmBatch→Execute chained in RemainingBatchModal
- 配置 entry gated by new CERT_SETTINGS_PERMISSION (cert:settings) additive constant in cert-permission.ts (single alignment point for 7.2); confirm-type actions gated by cert:manage facade with backend EIAM as authority
- Removed 6.1 placeholders wizard.vue/report.vue; routes repointed to new/index.vue and detail/index.vue per task file list

## Test Results
- **Tests Executed**: Yes
- **Passed**: 229
- **Failed**: 0
- **Coverage**: 0.0%

## Acceptance Criteria
- [x] 列表页：状态 Tab 全集（待确认含草稿；已回滚/回滚失败合并可按徽章区分）；列（单号/旧→新[旧侧保护期徽章]/状态徽章/进度/发起人/时间/操作）；执行中/验证中行 10s 轮询刷新；配置���口（仅主管）；空态新建变更 CTA
- [x] 7 步向导：步骤条高亮/只读回看徽章+返回当前步骤/未到达不可点；下一步闸门（Step1 双选/Step2 双预检/Step3 清单/Step4 确认）；Step1-4 存草稿、任意步取消二次确认；?certId= 预选（不合规置空+提示不锁不跳步）
- [x] Step2 预检自动执行整步 loading；阻断卡（扫描超期→立即扫描 / SAN / 在途互斥跳转 / 仅指纹提示）；Step3 可执行表格+不可执行分区（Warning 底色+原因+出路）+盲区横幅+快照时间；Step3 失败可重试可返回
- [x] Step4 确认：影响面 Modal 二次确认；分批首批上限 floor(总量/2) 超限禁用；快照重校验失败 Error 引用清单已变化+自动回退 Step2
- [x] Step5 2s 轮询逐项状态（成功/失败/执行中/限流重试 Warning）；失败项→横幅+回滚 Modal（目标有效性预检+转人工提示）；Step6 倒计时+逐项验证徽章+回滚入口+执行剩余批入口；Step5→6 终态自动进、Step6→7 窗口关闭自动进（提前完成二次确认）
- [x] 报告详情页：5 卡（清单/逐项结果[批次+结果筛选]/回滚状态/验证结论[未达标清单+窗口结论]/孤儿清理）+导出+只读恢复视图（执行中内嵌 Step5/验证中内嵌 Step6 重入轮询恢复；部分完成回滚入口+保护期徽章；草稿继续编辑；执行剩余批人工确认）；a11y 按跨小时档/终态事件通告与 reduced-motion 策略
- [x] Hard Rule：回滚入口仅 执行中(失败后)/验证中/部分完成 三态可见且仅成功项
- [x] Hard Rule：确认操作仅工程师可见可用，全程调用留审计的服务端端点
- [x] Hard Rule：执行期间清单快照固定，无任何执行中增删清单项入口

## Notes
验证：npx vitest run 全量 10 文件 229 通过（新增 changes/format.test.ts 57 例：Tab 全集与合并/进度派生/分批上限/7 步闸门与只读回看/阻断卡映射/清单分区出路/徽章/倒计时 a11y 跨小时档/回滚 Hard Rule/恢复模式/报告卡筛选结论/草稿持久化/预选资格）；npx vue-tsc -b 零错误；npm run build:check exit 0（vue-tsc+vite build；resource/tag 常量赋值与 chunk 体积警告为既有非本任务文件）；eslint 新增/修改文件零违规（0 errors 0 warnings）。coverage 记 0.0（vitest 未接 coverage provider，同 6.1/6.2/6.4 口径），可测逻辑层 format.ts 全部导出均有直接单测。API 口径适配（列表行无 progress/protect 字段、无逐项验证端点、无草稿端点、CHANGE_IN_FLIGHT 无冲突单号）均已在 format.ts 头注与 keyDecisions 文档化。跨仓库：任务文件在 e-cam-service，前端代码位于 D:\Haven\e-cam-web；按任务指示未提交 git（Do NOT commit）。
