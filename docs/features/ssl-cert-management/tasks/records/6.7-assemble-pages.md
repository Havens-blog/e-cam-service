---
status: "completed"
started: "2026-08-19 04:50"
completed: "2026-08-19 05:01"
time_spent: "~11m"
---

# Task Record: 6.7 页面装配（6 页面路由/组合/导航注册，Page Composition 表全页落地）

## Summary
页面装配核验收口：Page Composition 表 7 路由全部渲染 6.2-6.6 真实页���（占位已在前期任务替换完毕，本次以路由→组件文件存在性测试固化）；导航注册抽出为纯数据模块 cert-nav.ts 并由 MainLayout 消费（顺序/目标/只读可见性/图标落 iconfont 均测试锚定）；跨页流转五条链路核验打通（台账行→详情、发起更换→向导 ?certId= 预选不锁不跳步、变更列表行→报告[草稿恢复向导]、配置入口→设置页仅主管、看板抽屉→证书详情全角色）；只读拦截端到端核验（菜单过滤+路由守卫 certManageOnly×5+详情只读模式）；空态链路与既有页面回归通过（vitest 289/289、build:check exit 0、router 三文件 diff 零删除行）

## Changes

### Files Created
- e-cam-web/src/utils/cert-nav.ts
- e-cam-web/src/utils/cert-nav.test.ts

### Files Modified
- e-cam-web/src/router/routes.test.ts
- e-cam-web/src/layouts/MainLayout.vue
- e-cam-web/tsconfig.app.json

### Key Decisions
- SPEC CONTRADICTION：任务 AC 写「报告页配置→设置页」，prd-ui-functions Secondary Pages 表与 ui-design 列表页头均定义配置入口在变更管理列表页头（仅主管/审计）——按 Reference Files 权威落地于 /certs/changes 页头 hasCertSettingsAccess 门控，设置页面包屑按 ui-design 返回变更管理
- 导航注册从 MainLayout 内联数据抽为 src/utils/cert-nav.ts 纯数据模块（仅组合与路由级接线，未动 6.2-6.6 组件内部逻辑），使菜单顺序/只读可见性/图标可回归测试
- routes.test.ts 以懒加载函数字符串→磁盘文件存在性断言固化「7 路由渲染真实组件非占位页」，并断言无 Page Composition 表外证书路由（Hard Rule）
- 跨页流转与空态链路为页面内联 handler（仓库测试约定 node 纯逻辑测试，不挂载组件），以代码走查 file:line 证据核验；tsconfig.app.json 增加 node types 以支持测试内 node:fs 断言

## Test Results
- **Tests Executed**: Yes
- **Passed**: 289
- **Failed**: 0
- **Coverage**: 0.0%

## Acceptance Criteria
- [x] Page Composition 表 7 路由全部渲染真实组件（非占位页），每页与 ui-design Component 定义一致
- [x] 跨页流转打通：台账行→详情（面包屑回台账）；台账发起更换→向导 ?certId= 预选；变更列表行→报告详情（面包屑回列表）；配置入口→设置页（仅主管可见，按 prd/ui-design 落在变���管理页头）；看板抽屉查看证书详情→/certs/:id
- [x] 只读角色端到端核验：仅到期看板菜单；5 条 certManageOnly 路由拦截提示；/certs/dashboard 与 /certs/:id 可达且详情只读模式
- [x] 导航注册核对：三一级菜单图标与顺序按 prd-ui-functions；面包屑/返回导航按 Secondary Pages 表
- [x] 全模块构建与既有页面回归：build:check 通过；router diff 零删除行，既有路由无回归
- [x] 空态链路：无证书台账 empty→批量导入引导；无变更单列表 empty→新建变更引导

## Notes
跨仓库：实际改动均在 e-cam-web（未提交，按指示 Do NOT commit）；e-cam-service 侧无代码变更。验证：npx vitest run 全量 12 文件 289 用例通过（含本任务新增 cert-nav.test.ts 5 例 + routes.test.ts 扩展 2 例）；npm run build:check（vue-tsc -b && vite build）退出码 0；eslint（cert-nav/routes.test/MainLayout）零错误；router 三文件 diff 零删除行（既有路由无回归，/dashboard、/accounts、/iam/users 抽验在册）。coverage=0.0：e-cam-web 未安装 @vitest/coverage-v8（无 coverage provider，沿用 6.2-6.6 记录口径）。build 含两类存量告警（resource/tag esbuild 常量赋值提示、chunk>1500kB），非本任务引入。任务文件 just compile/fmt/lint 为 Go 仓库门禁，本任务未改 e-cam-service 代码故不适用，以上述前端等价门禁替代
