---
status: "completed"
started: "2026-08-19 03:20"
completed: "2026-08-19 03:31"
time_spent: "~11m"
---

# Task Record: 6.1 前端模块脚手架（cert 路由注册、菜单、EIAM 权限、只读角色路由拦截、api client 封装）

## Summary
e-cam-web 证书管理模块脚手架：注册 7 条 /certs 路由（懒加载+meta，二级页 hideInMenu），MainLayout 新增证书管理菜单组（台账/到期看板/变更管理，requireCertManage 按 EIAM 授权码+isAdmin 过滤），全局路由守卫实现只读角色拦截（certManageOnly 标记路由提示「无权限访问，仅到期看板可见」并回退 /certs/dashboard，dashboard 与 /certs/:id 开放），新增 src/api/cert.ts 覆盖 cert 全部 30 个端点的类型化 client（专用 certAxios 实例解 {success,data,error,meta} 信封，业务/HTTP 错误统一转 CertRequestError 保留字符串错误码；类型含 ChangeStatus 9 态/ProbeStatus 6 值/ReferenceStatus 3 值/ExpiryAlertLevel 5 值/BatchSessionStatus 3 值等枚举全集，字段名镜像后端 web 层 VO json tag），7 个占位页面组件（6.2-6.6 填充）。跨仓库落地：任务文件在 e-cam-service，代码全部位于 D:\Haven\e-cam-web。

## Changes

### Files Created
- e-cam-web/src/api/request/cert.ts
- e-cam-web/src/api/cert.ts
- e-cam-web/src/utils/cert-permission.ts
- e-cam-web/src/utils/cert-permission.test.ts
- e-cam-web/src/api/cert.test.ts
- e-cam-web/src/router/routes.test.ts
- e-cam-web/src/views/cert/ledger/index.vue
- e-cam-web/src/views/cert/detail/index.vue
- e-cam-web/src/views/cert/dashboard/index.vue
- e-cam-web/src/views/cert/changes/index.vue
- e-cam-web/src/views/cert/changes/wizard.vue
- e-cam-web/src/views/cert/changes/report.vue
- e-cam-web/src/views/cert/settings/index.vue

### Files Modified
- e-cam-web/src/router/routes.ts
- e-cam-web/src/router/types.ts
- e-cam-web/src/router/index.ts
- e-cam-web/src/layouts/MainLayout.vue

### Key Decisions
- 信封冲突处理：主 request 实例全局拦截器按 {code} 解包会拒绝 cert 的 {success,data,error,meta} 信封且 interceptorsToOnce 无法在其后 rescue，故仿照 eiamAxios（ginx 信封）先例新增 certAxios 专用实例（同 baseURL/token/401 机制），信封解包收敛在 unwrapCertEnvelope——纯增量，零改动既有模块行为（Hard Rule：additive）
- 角色判定：7.2（EIAM 资源同步）未实施，eiam profile 目前仅暴露授权码数组+is_admin；定义唯一对齐点 CERT_MANAGE_PERMISSION='cert:manage'（平台既有 '<domain>:<...>' 风格），isAdmin||持码=可管理，否则视为只读查看者（deny-by-default，与后端 RequireRoles 未设置即拒绝一致）——7.2 定稿资源码后仅需改 cert-permission.ts 一处
- 菜单机制：实际代码为 MainLayout.vue 硬编码 menuGroups（非 routes meta 派生），按任务要求沿用现有机制新增证书管理分组；可见性复用 requireAdmin 同构的 requireCertManage 标志，过滤谓词在无新标志时与原行为完全一致
- 模块目录按任务文件落地 src/views/cert/（与 views/cam 平级），覆盖 tech-design 中 views/modules/cert/ 的表述（任务文件优先）；7 条路由对应 7 个组件文件（changes/ 下 index+wizard+report）
- api 类型字段名逐一对照已实现后端 internal/cert/web/*.go 的 json tag（ledger/dashboard/change/settings/reference/cert handler），multipart 字段名 certFile/keyFile/expectedDomain/certFiles/keyFiles 与后端读取一致

## Test Results
- **Tests Executed**: Yes
- **Passed**: 48
- **Failed**: 0
- **Coverage**: 0.0%

## Acceptance Criteria
- [x] src/router/routes.ts 注册 7 条路由（懒加载 + meta.title/icon，二级页 hideInMenu）：/certs、/certs/:id、/certs/dashboard、/certs/changes、/certs/changes/new、/certs/changes/:id、/certs/settings
- [x] 一级菜单证书管理分组下展示 证书台账/到期看板/变更管理 三项（沿用现有菜单机制，按 EIAM 角色控制可见性）
- [x] 只读角色：仅见到期看板菜单；/certs、/certs/changes*、/certs/settings 直接访问被前端拦截提示；/certs/dashboard 与 /certs/:id 开放
- [x] src/api/cert.ts：覆盖 cert 全部端点的类型化 client（请求经现有 request 封装，响应解 {success,data,error,meta} 信封），类型定义含枚举全集
- [x] src/views/cert/ 模块骨架建立，构建（type-check/build）无错误

## Notes
验证：vitest 全量 48 通过（新增 14：cert-permission 4 + routes 6 + cert 信封解包 4——覆盖 AC1/AC3 拦截集合精确断言与 AC4 解包矩阵）；npm run build:check（vue-tsc -b && vite build）exit 0；eslint 对新增/修改文件零违规。前端拦截仅为体验层，接口侧后端 RequireRoles 已就位（EIAM claims 接线在 7.2）。前端只读判定当前对非管理员非授权码用户默认收敛为 viewer（7.2 同步授权码后即按码放行）。未提交 git（按任���指示 Do NOT commit）。
