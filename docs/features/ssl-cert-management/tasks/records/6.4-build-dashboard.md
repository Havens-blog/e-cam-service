---
status: "completed"
started: "2026-08-19 04:02"
completed: "2026-08-19 04:15"
time_spent: "~13m"
---

# Task Record: 6.4 到期看板组件（UF-3：5+2 卡联动筛选/探测四态/抽屉）

## Summary
e-cam-web 到期看板页（UF-3）全部落地：index.vue 四态页面（loading 骨架 5+2 卡+5 行/800ms 延迟显示、empty「暂无证书，等待导入」无操作入口、error 重试、populated）+ OverviewCards（5 级互斥分桶卡 countsByLevel 序 + 差异告警数/探测豁免数次级卡，同等卡片形态，点击联动表格过滤、选中 Accent 高亮 aria-pressed、再点取消）+ DashboardTable（工具栏 状态分级/云多选[referencedClouds 命中其一]/托管类型 客户端过滤 + 最近巡检 + 通配符跳过计数 meta；列：子域名[云 chips]/剩余天数[状态色徽章]/托管类型/线上探测[四态徽章+补充枚举，差异 tooltip 附最近探测时间]/豁免[✓/—]；小屏隐藏托管类型/豁免列）+ ProbeDetailDrawer（证书要素+最近探测详情[时间/线上指纹/台账指纹 mono 截断+复制/差异说明]+底部仅 复制差异摘要 与 查看证书详情[/certs/:certId] 两入口，只读兼容；筛选变化 aria-live polite 通告）。纯逻辑抽取 src/views/cert/dashboard/format.ts（10 函数）node 单测 34 例。为满足 AC4 抽屉数据对 e-cam-service 做增量扩展：DashboardItem/VO 增加 certId/fingerprint/lastProbeAt/onlineFingerprint（4.1 先例：spec AC 优先于 api-handbook 既有 items 形状，additive 向后兼容），api-handbook 契约行同步更新。

## Changes

### Files Created
- e-cam-web/src/views/cert/dashboard/format.ts
- e-cam-web/src/views/cert/dashboard/format.test.ts
- e-cam-web/src/views/cert/dashboard/components/OverviewCards.vue
- e-cam-web/src/views/cert/dashboard/components/DashboardTable.vue
- e-cam-web/src/views/cert/dashboard/components/ProbeDetailDrawer.vue

### Files Modified
- e-cam-web/src/views/cert/dashboard/index.vue
- e-cam-web/src/api/cert.ts
- e-cam-service/internal/cert/service/dashboard_service.go
- e-cam-service/internal/cert/web/dashboard_handler.go
- e-cam-service/internal/cert/web/dashboard_handler_test.go
- e-cam-service/docs/features/ssl-cert-management/design/api-handbook.md

### Key Decisions
- 后端增量扩展（SPEC CONTRADICTION 裁决）：ui-design 抽屉 Data Binding 要求 lastProbeAt/onlineFingerprint + 查看证书详情需 certId，api-handbook 既有 items 不含——按 4.1 先例以 AC+ui-design 为准 additive 扩展 DashboardItem/VO（certId/fingerprint/lastProbeAt/onlineFingerprint），向后兼容，api-handbook 行同步
- probeStatus 渲染：四态严格按 ui-design 渲染定义（一致=Success✓/差异=Error✗+最近探测 tooltip/不可达=Secondary⚠ 不参与差异告警/豁免=Secondary✓），补充 wildcard_skipped=Secondary◇通配符跳过、change_linked_diff=Warning 变更关联（预期切换非事故）、空串=Secondary 未探测；本页无 Verifying 紫
- 差异说明为前端按状态文案（ProbeResult 无服务端 reason 字段，er-diagram/tech-design 确认；原型 dashboard.html 口径），复用进 diffSummaryText 复制摘要
- Hard Rule 落地：special=diff 过滤仅匹配 probeStatus=diff（不可达/豁免/通配符/变更关联/一致/未探测均不计，单测覆盖）；卡片副文案显式声明口径；只读页面零变更类操作入口（空态无导入 CTA、抽屉仅复制摘要+查看详情两入口+只读说明）
- filter 为唯一状态源：5 级卡与工具栏状态分级下拉共享 filter.level（互相同步），special[diff/exempt 卡]与三维正交 AND 组合；wildcardSkippedCount 于工具栏 meta 可见（tooltip 显式缺口口径），三 rate 字段由 DashboardSummary 类型/API 契约承载（看板布局规范无覆盖率卡，覆盖率卡属台账页 UF-1）
- 复用纯模块避免重复：ledger/format（daysLeftBadge/hostingStatusMeta/truncateFingerprint/copyText/resolvePageState）与 detail/format（cloudLabel）跨视图 import（纯函数模块无 Vue/DOM 依赖，DRY）

## Test Results
- **Tests Executed**: Yes
- **Passed**: 172
- **Failed**: 0
- **Coverage**: 0.0%

## Acceptance Criteria
- [x] 5 总览卡 + 差异告警卡 + 豁免卡（同等卡片形态）：点击卡过滤表格、选中 Accent 高亮再点取消；summary 含 countsByLevel[5]/diffAlertCount/exemptCount/wildcardSkippedCount + 三 rate 字段
- [x] 表格列：子域名(云 chips)/剩余天数(状态色徽章)/托管类型/线上探测(probeStatus 四态)/豁免；筛选 状态分级/云多选(命中其一)/托管类型；最近巡检时间
- [x] probeStatus 渲染：一致=Success✓；差异=Error 角标+「差异」+最近探测时间 tooltip；不可达=Text Secondary+⚠（不参与差异告警）；豁免=Secondary+✓；非色觉通道（图标+文字）
- [x] 行点击→右抽屉：证书要素+最近探测详情(时间/线上指纹/差异说明)+复制差异摘要+查看证书详情链接（跳转 /certs/:id，全角色可达）
- [x] 只读查看者：本页无变更类操作入口；仅保留复制差异摘要与查看证书详情；差异告警由巡检自动触达（抽屉内只读说明）
- [x] 四态齐备：loading/empty(暂无证书，等待导入)/error/populated；差异行角标；a11y 状态卡 aria-live polite 通告筛选变化

## Notes
验证：前端 npx vitest run 全量 172 通过（新增 dashboard/format.test.ts 34 例：卡片联动筛选/Hard Rule 差异口径/四态徽章/复制摘要/通告文案/相对时间）；npm run build:check（vue-tsc -b && vite build）exit 0；eslint 新增/修改文件零违规；后端 go vet + go test ./internal/cert/... 全量通过（新增 TestDashboard_ItemProbeDetail + 未探测域抽屉字段断言）。coverage 记 0.0（vitest.config 未接 coverage provider，同 6.1/6.2 口径）；可测逻辑层 format.ts 全部函数均有直接单测。gofmt -l 命中为全仓 CRLF 基线噪声（未触碰文件同样命中），本次修改保持文件既有 CRLF，无新增格式偏离。跨仓库：任务文件在 e-cam-service，前端代码位于 D:\Haven\e-cam-web；e-cam-service 非 git 仓库、e-cam-web 按任务指示未提交 git（Do NOT commit）。写盘过程中两处中文注释字符经校验修复（工具编码毛刺，已全文件 U+FFFD 扫描确认干净）。
