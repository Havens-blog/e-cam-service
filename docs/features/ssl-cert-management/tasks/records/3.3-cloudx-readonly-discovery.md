---
status: "completed"
started: "2026-08-18 19:01"
completed: "2026-08-18 19:58"
time_spent: "~57m"
---

# Task Record: 3.3 华为云/AWS/Azure 只读发现（discovery-only CloudDeployer，仅 ListReferences/GetCert，ERR_DISCOVERY_ONLY）

## Summary
为华为云/AWS/Azure 新增 discovery-only 证书发现适配（tech-design Interface 2: CloudDeployer）：仅实现 ListReferences/GetCert 只读方法，UploadCert/BindResource/CleanupOrphan 一律返回哨兵 ErrDiscoveryOnly（新增于 cloudx 公共 cert_errors.go，与 ErrCloudRateLimited 同处）。华为云（CDN 域名证书/WAF 防护域名/ELB v3 监听器按协议映射 alb|nlb，GetCert 走 SCM）与 AWS（CloudFront/ELBv2 application|network，GetCert 走 ACM PEM→SHA256）落在既有目录新增 cert_discovery.go；Azure 无既有基座，新建 internal/shared/cloudx/azure/ 最小适配（net/http 直调 ARM/Key Vault REST，client_credentials 令牌按 scope 缓存，复用云账号 ClientID/Secret，租户/订阅经 Option 或 AZURE_TENANT_ID/AZURE_SUBSCRIPTION_ID 回退）。全部只读 API，无任何云侧写调用；既有 huawei/aws 适配签名未动（additive）。

## Changes

### Files Created
- internal/shared/cloudx/huawei/cert_discovery.go
- internal/shared/cloudx/huawei/cert_discovery_test.go
- internal/shared/cloudx/aws/cert_discovery.go
- internal/shared/cloudx/aws/cert_discovery_test.go
- internal/shared/cloudx/azure/cert_discovery.go
- internal/shared/cloudx/azure/cert_discovery_test.go

### Files Modified
- internal/shared/cloudx/cert_errors.go
- go.mod
- go.sum

### Key Decisions
- ErrDiscoveryOnly 定义在 cloudx 公共 cert_errors.go（任��注释指定与 ErrCloudRateLimited 同处），三包再导出以对齐 3.1/3.2 调用形态
- 华为云 ELB 无独立 alb/nlb 产品线：按监听协议归类（HTTPS/TERMINATED_HTTPS→alb，TLS→nlb），枚举对齐 schema.sql cert_references.product
- 华为云 CDN 证书无独立证书 ID，以 cert_name 作 referencedCloudCertId；SCM 指纹为 SHA-1 形态，与台账 SHA256 口径不一致，上层按无法复核处理（同 3.2 腾讯云口径，PoC 5.12 登记验证）
- AWS WAF 非 TLS 终结点、Azure WAF policy 附加于 Front Door/App Gateway 且自身无证书引用面、Azure Load Balancer 为四层无 TLS 终结：三者均不入支持集并在代码注释载明（首期盲区声明）
- AWS GetCert 仅覆盖 ARN 形态（ACM），CloudFront 引用的 IAM 托管证书 ID 显式报错不覆盖
- Azure 内联上传证书（App Gateway data 字段）无云侧证书 ID：跳过并计数为发现盲区（视图层声明口径）
- 新增依赖 aws-sdk-go-v2/service/acm v1.38.0（该版本 go.mod 恰要求 core v1.41.5，与既有版本完全一致，不升级任何既有依赖）
- 华为云 SDK SafeBuild 经 IAM 自动解析 domain/project id（网络调用），与既有华为云适配器同行为，单测仅覆盖不触网的地域校验失败路径

## Test Results
- **Tests Executed**: Yes
- **Passed**: 58
- **Failed**: 0
- **Coverage**: 84.4%

## Acceptance Criteria
- [x] huawei/aws/azure 三云各新增 discovery 适配：ListReferences(product) 返回 CloudCertRef 列表（含 referencedCloudCertId），GetCert 返回 CloudCertInfo
- [x] UploadCert/BindResource/CleanupOrphan 统一返回 sentinel ErrDiscoveryOnly（ERR_DISCOVERY_ONLY 语义），不得产生任何云侧写操作
- [x] 三云凭证复用既有 cloudx 账号体系（azure 目录缺失则新建 internal/shared/cloudx/azure/ 最小适配）
- [x] 产品覆盖：三云的 CDN/WAF/LB 证书引用面（各云以实际产品映射，枚举对齐 cert_references.product）
- [x] mock 单元测试：ListReferences/GetCert 成功路径 + 三写方法返回 ErrDiscoveryOnly 断言
- [x] go build ./internal/shared/cloudx/... 全绿，additive

## Notes
验证：go build ./... 全绿；go vet ./internal/shared/cloudx/... 通过；三包全量测试 ok（含既有包无回归）；新文件 gofmt 干净（gofmt -l 命中的 aliyun/* 为既有文件非本次改动）。覆盖率为三份 cert_discovery.go 按函数均值（huawei 83.3% / aws 88.0% / azure 81.9%，azure 包语句级 85.0%，取均值 84.4%）。共 58 个子测试通过。
