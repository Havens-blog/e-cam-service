---
status: "completed"
started: "2026-08-18 17:15"
completed: "2026-08-18 17:30"
time_spent: "~15m"
---

# Task Record: 2.2 导入服务与 API（单张+批量+补传私钥升级、重复指纹 409、expectedDomain 提示比对、batchId 会话、进度轮询）

## Summary
实现证书导入服务与 API：单张导入（multipart certFile+可选 keyFile+可选 expectedDomain，信封加密落库）、批量导入（CertBatchSession 会话持久化 + 逐文件隔离 recover + 进度轮询终态收敛）、补传私钥升级完整托管（重跑 2.1 匹配校验）；统一 {success,data,error,meta} 信封与 CERT_* 错误码→HTTP 映射（400/409/404），私钥与密文任何接口不返回。

## Changes

### Files Created
- internal/cert/service/import_service.go
- internal/cert/web/cert_handler.go
- internal/cert/web/response.go
- internal/cert/web/router.go
- internal/cert/certtest/certtest.go
- internal/cert/service/import_service_test.go
- internal/cert/web/cert_handler_test.go

### Files Modified
- internal/cert/domain/certificate.go
- internal/cert/domain/repository.go
- internal/cert/domain/models_test.go
- internal/cert/repository/certificate.go
- internal/cert/repository/certificate_test.go
- docs/features/ssl-cert-management/design/schema.sql

### Key Decisions
- SPEC GAP 补齐：schema.sql 原未持久化证书材料，补传私钥无法重跑 2.1 匹配校验——新增 cert_certificates.certPem（导入束原文落库，仅校验/云上传依据，永不出现在 API 响应），schema.sql 与 models_test 预期清单同步更新保持 1:1
- expectedDomain 提示放响应 meta.expectedDomainMissing（Hard Rule 要求 data 仅 certId/fingerprint/hostingStatus 三字段）
- 批量导入按 goroutine 派发（会话先持久化再异步处理，浏览器中断后轮询仍可见）；逐文件 recover 兜底 panic 落 INTERNAL_ERROR errorReason；批处理整体 10min 超时防泄漏
- 批量证书/私钥按去扩展名基名配对（a.crt↔a.key，小写），同名冲突取首个
- CertificateRepository 扩展 GetByID/AttachPrivateKey（密文写入与 hostingStatus=complete 同一原子 update）
- web 层错误码映射用固定安全文案（不回显 err.Error()），500 详情仅进日志
- 测试经 internal/cert/certtest 共享夹具（现场生成 PKI）+ 内存假仓储，无 MongoDB 依赖确定性运行；仓储新增方法另补集成冒烟（无 DB 时按既有惯例跳过）

## Test Results
- **Tests Executed**: Yes
- **Passed**: 36
- **Failed**: 0
- **Coverage**: 86.5%

## Acceptance Criteria
- [x] POST /api/v1/certs：multipart 成功 200 {certId,fingerprint,hostingStatus}；keyFile→complete（信封加密），无 keyFile→fingerprint_only；expectedDomain 不一致仅提示不拦截
- [x] 校验失败映射：CERT_KEY_MISMATCH/CERT_CHAIN_INCOMPLETE/CERT_PARSE_FAIL→400；重复指纹→409 CERT_DUPLICATE_FINGERPRINT；逐项错误码与 api-handbook 一致
- [x] POST /api/v1/certs/batch→202 {batchId,status,files[],progress}；batchId=cert_batch_sessions._id；逐文件隔离，失败行含 errorReason 可单独重试
- [x] GET /api/v1/certs/batch/:batchId 同构响应；终态 completed/partial_failed；progress 原子递增；TTL 30 天由索引承接（Phase 1 ttl_batch_session_30d）
- [x] POST /api/v1/certs/:id/key：匹配校验通过升级 complete（加密落库）；不匹配→400 CERT_KEY_MISMATCH
- [x] httptest 契约测试：单张四类拦截、批量混合成败、轮询到终态、补传升级；所有响应断言无明文私钥/密文字段
- [x] Hard Rule：任何响应/日志不返回明文私钥与密文；导入成功响应仅 certId/fingerprint/hostingStatus
- [x] Hard Rule：批量导入逐文件隔离，panic recover + errorReason 落档不中断会话

## Notes
coverage 为 service 86.5% / web 84.9% / domain 97.0%（repository 集成测试无本地 MongoDB 时按既有惯例跳过）。gin v1.11 静态段 /batch 与参数段 /:id 同级共存验证通过。ioc/wire 装配不在本任务文件清单，路由注册入口为 web.RegisterRoutes(server, h)，供后续接线任务使用。
