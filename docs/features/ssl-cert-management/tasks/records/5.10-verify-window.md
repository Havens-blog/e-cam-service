---
status: "completed"
started: "2026-08-19 02:30"
completed: "2026-08-19 02:52"
time_spent: "~22m"
---

# Task Record: 5.10 验证窗口服务（verifyExpected+excludedDomains、提频探测 verifyConfirmProbes、window-expiry 终局判定、UnmetDomains）

## Summary
实现验证窗口服务：批执行完成固化 verifyExpected（豁免命中剔除入 excludedDomains 计 skipped，windowUntil=now+verifyWindowHours 与 verifyWindowUntil 原子对齐，分批每批覆盖刷新，固化后不随台账变化）；ProbeVerifyingWindows 提频探测 verifyExpected.domains（复用 4.1 ProbeDomains），连续 verifyConfirmProbes 次线上指纹=预期指纹=达标（终批提前关闭→completed / 非终批→批间暂停 paused/pausedAt 等续批）；FinalizeExpiredWindows scheduler 主动扫描终局判定（全达标→completed；未达标→partial_completed+UnmetDomains 写入 Recorder 端口+恢复常规告警；非终批→批间暂停转人工决策）；BatchVerified 实现 5.7 BatchVerifyChecker（ConfirmBatch 门控 2），SealVerifyExpected 经新增 VerifyWindowSealer 缝接入 enterVerifyWindow（EnterVerify 后立即固化，nil 安全降级为调度惰性补固化）；change_linked 告警经 VerifyWindowContext 路由（窗口内 Active=true 附 orderId/预期指纹/达标计数且状态迁移去重，关闭后 Active=false 恢复常规通道）；无 override 通配符验证项计 skipped 不阻塞达标。新增仓储原子原语 SetVerifyExpected/PauseAfterVerify/ListVerifyingExpired（CAS 语义）与 ListRecentByDomains（连续一致判据数据源），mongo 实现经可达实例集成验证。

## Changes

### Files Created
- internal/cert/service/verify_window_service.go
- internal/cert/service/verify_window_service_test.go

### Files Modified
- internal/cert/domain/repository.go
- internal/cert/repository/change_order.go
- internal/cert/repository/probe_result.go
- internal/cert/repository/change_order_test.go
- internal/cert/certtest/change_fakes.go
- internal/cert/certtest/certtest.go
- internal/cert/service/execute_service.go
- internal/cert/service/execute_service_test.go
- internal/cert/service/probe_service_test.go

### Key Decisions
- verifyExpected.domains 基准 = 旧证书 SAN 集合（5.2 SAN 预检同口径：清单项所服务域名并集）——数据模型无逐资源域名映射，per-batch 刷新语义落在 windowUntil/豁免清单时点快照上，固化缝保证每批进入 verifying 时覆盖重算
- 达标判定按指纹不按状态枚举：onlineFingerprint==newCertFingerprint 即计一致（覆盖新证已入台账的 consistent 与未入台账的 change_linked_diff 两种形态），连续 N 次判据取每域最近 N 条 probeAt 降序记录
- 非终批窗口收敛（提前达标或到期）统一 PauseAfterVerify 回 executing+paused（当前批保持）——Cancel/回滚的人工决策入口在批间暂停态可用，ConfirmBatch 门控 2 仍拦截未达标续批（不自动续批）
- SetVerifyExpected 返回 MatchedCount 而非 ModifiedCount：Mongo 对同值重写 ModifiedCount=0，幂等重固化不应误判为并发迁移（真实例集成测试踩实）
- change_linked 告警仅在新转入 change_linked_diff 的域名发布（前次状态非 change_linked_diff 或无历史，状态迁移事件语义），持续差异不重复告警；UnmetDomains/终态次序=终态先落地再记录/告警，失败上抛不回滚（下轮不再命中）
- 7.1 装配点：ProbeVerifyingWindows（周期=verifyProbeIntervalMinutes）与 FinalizeExpiredWindows（同周期）为调度入口；sealer/verify/recorder/publisher 均为注入端口，7.x 接线 ChangeService 满足 VerifyWindowTransitions

## Test Results
- **Tests Executed**: Yes
- **Passed**: 13
- **Failed**: 0
- **Coverage**: 80.3%

## Acceptance Criteria
- [x] 固化 verifyExpected：批执行完成时按该批目标域名构建 domains[]，剔除当前豁免清单命中域名写入 excludedDomains[]（豁免项验证计 skipped 不参与达标判定），设 windowUntil=now+verifyWindowHours；分批单每批按该批域名刷新
- [x] 提频探测：订单 verifying 期间对 verifyExpected.domains 探测周期由天级提至 verifyProbeIntervalMinutes；连续 verifyConfirmProbes 次一致=批级达标 → 批间暂停（paused=true/pausedAt）等续批或窗口提前达标关闭
- [x] window-expiry 终局判定函数 FinalizeExpiredWindows(ctx)：扫 verifyWindowUntil <= now 且 status=verifying 单据——全部达标→completed；存在未达标→partial_completed + 未达标域名写入 ChangeReport.UnmetDomains（Verify.Unmet 计数）+ 恢复常规 diff 告警
- [x] change_linked_diff 路由：窗口内 diff 且 domain∈verifyExpected.domains 且线上指纹=newCertFingerprint → 走变更关联通道（4.3 verifyWindowRoute）；窗口关闭后恢复常规 diff 判定
- [x] 豁免∩窗口不死锁：豁免域名进 excludedDomains 计 skipped，窗口仍可正常达标（含通配符无 override 也计 skipped）
- [x] 集成测试：豁免∩窗口达标、提前达标关闭、window-expiry 转 partial_completed+UnmetDomains、change_linked_diff 路由、分批 verifyExpected 刷新

## Notes
测试：新增 12 个 service 测试函数（verify_window_service_test.go，含 5.7 执行引擎真集成用例 TestVerifyWindow_ExecuteEngineIntegration：ConfirmBatch 门控 2 联调+固化后不随台账变化+分批刷新）+1 个仓储集成测试（TestChangeOrderRepo_VerifyWindowPrimitives，经 CERT_TEST_MONGODB_DSN 可达实例 2 次通过，夹具随机库+Cleanup Drop）；go test ./internal/cert/... 全绿（无 -race：宿主无 cgo，并发正确性靠 CAS 原子原语+评审，见仓内既有口径）。coverage=service 包 80.3%（verify_window_service.go 函数级 66.7%~100%）。门禁：go build ./... PASS；go vet ./... PASS；gofmt 新建文件 clean——WARNING 非阻断：co-edit 既有文件 certtest.go/probe_result.go/probe_service_test.go 为既有 CRLF 检出噪声、change_fakes.go/change_order.go 为既有 //（ 注释行（未触碰的 change_service.go 同类命中佐证先存）。仓储集成测试另见 2 个记忆已录的既有失败（TestCertificateRepo_ListPage 夹具缺陷/GetByIDAndAttachPrivateKey 远端读己之写延迟）与远端实例偶发 socket EOF，均非本任务代码。7.1 待办：调度注册两入口周期=verifyProbeIntervalMinutes；VerifyWindowRecorder 生产接线（audit action）供 5.11 报告聚合 Verify.Unmet/UnmetDomains。
