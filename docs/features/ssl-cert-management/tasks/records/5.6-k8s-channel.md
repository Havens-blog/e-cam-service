---
status: "completed"
started: "2026-08-19 00:59"
completed: "2026-08-19 01:21"
time_spent: "~22m"
---

# Task Record: 5.6 K8sAPIChannel（三信号管理权探测、patch 仅证书字段、recheckDelayMinutes 复检、AutoChangeable 标记）

## Summary
实现 K8sAPIChannel（ExecutionChannel 的 K8s 实现）：Discover 按固定枚举（ALBConfig/Ingress/Gateway/HTTPRoute，共享 3.4 BuiltinRegistrations 常量表）+ enabled=true CrdRegistration 遍历，按 certFieldPath 读证书引用字段产 CertReference（product=crd，含 clusterId/namespace/kind，指纹映射通配反查+确定性占位指纹与 3.5 同口径），enabled=false 登记经 CRDBlindSpots 盲区声明；三信号管理权探测（GitOps label 精确键+前缀可配置/ownerReferences 非空/cert-manager annotation，任一命中 manageable=false + Reason 记信号类型+具体键），经 Probe 方法满足 5.2 ManagementProbe 端口（service 包新增编译期绑定断言）；Deploy 前 fingerprint→cloudCertID 经 CloudCertMapping 解析（active 且互异唯一，0 条或歧义显式失败）+ 三信号复核（命中拒绝 patch）+ 执行前读 oldCloudCertId 保留 + JSON Patch replace 仅指向 certFieldPath 各叶子（Hard Rule：不整对象覆盖），K8S_UNREACHABLE 经 domain.ErrK8sUnreachable 透传；Rollback 同样仅 patch 证书引用字段，ErrCode 映射 K8S_UNREACHABLE；RecheckCRDField 单轮复检（仍为新证书 ID→passed，被 reconcile 回写旧值/其他值/字段缺失→failed+Reason 附 orderId/itemId 供 5.9 告警；Hard Rule：次数固定 1 失败转人工，延迟调度归 5.9/7.1）。fake dynamic client 全量覆盖（19 个测试函数/26 用例，deployer 包覆盖率 87.5%）。

## Changes

### Files Created
- internal/cert/deployer/k8s_api_channel.go
- internal/cert/deployer/k8s_api_channel_test.go

### Files Modified
- internal/cert/service/changelist_generator.go

### Key Decisions
- Deploy 接口签名固定只传 newCertFingerprint（Hard Rule），新证书 ID 解析归通道：CloudCertMapping.ListByFingerprint 取 active 且去重后唯一 cloudCertId，0 条或多云互异无法消歧时返回 ErrCloudCertIDUnresolved 显式失败（不猜测写入防错误证书播撒）
- CRDClient/CRDClientProvider 端口隔离 k8s.Factory（K8sFactoryClients 生产适配器），测试注入 fake dynamic client 并复刻连接类错误→ErrK8sUnreachable 包装语义
- 自定义 CRD 目标解析（Probe/Deploy/Rollback/Recheck）用 regs.List()（含 enabled=false——执行期清单快照固定不变，登记停用不阻断在途变更项），Discover 扫描范围才用 ListEnabled；GVR 候选探测（plural=lower(kind)+s × v1/v1beta1/v1alpha1）与 3.5 扫描网关同启发式（service 层版本不可导入，deployer 层镜像）
- RecheckCRDItem 为通道级复检输入载荷（Ref/OrderID/ItemID/NewCertID/OldCertID），由 5.9 crd-recheck 消费者构造；告警发送归 5.9，通道仅产出 RecheckResult+Reason（附 orderId/itemId）
- Deploy 返回 RecheckPassed=false 语义为待复检（bool 字段零值），终值由 crd-recheck 复检回填——对齐 tech-design DeployResult 注释

## Test Results
- **Tests Executed**: Yes
- **Passed**: 86
- **Failed**: 0
- **Coverage**: 87.5%

## Acceptance Criteria
- [x] Discover：固定枚举四类 + enabled=true CrdRegistration 遍历；certFieldPath 产 CertReference{product=crd,clusterId,namespace,kind,resourceId,referencedCloudCertId}；未登记/enabled=false CRD 盲区声明
- [x] 三信号管理权探测（GitOps label 默认 argocd/fluxcd 可 config、ownerReferences 非空、cert-manager annotation；任一命中 AutoChangeable=false + Reason 记信号类型+具体键）
- [x] Deploy：仅 patch 证书引用字段（JSON Patch 指向 certFieldPath 不动其他字段）；执行前读 oldCloudCertId 保留；K8S_UNREACHABLE→failed+error=K8S_UNREACHABLE；patch 产出 DeployResult{RecheckPassed=待复检}
- [x] RecheckCRDField(ctx,item)：单轮复检——仍为新证书 ID→RecheckPassed=true/success；reconcile 回写旧值/其他值→false/failed+告警上下文（附 orderId/itemId）；复检次数固定 1 失败转人工
- [x] AutoChangeable 标记回填：5.2 清单生成经 ManagementProbe 端口调本通道探测（service 包编译期绑定断言）
- [x] 测试：fake dynamic client 覆盖三信号各命中、patch 仅证书字段、复检通过/回写失败、盲区声明

## Notes
验证：go build ./... OK、go vet ./... OK、go test ./internal/cert/... 全绿（deployer 86 用例含本任务 19 函数/26 用例，覆盖率 87.5%≥80%）。golangci-lint 未安装（Makefile 定义跳过路径）。WARNING：仓库既有 ~90 个 .go 文件被 gofmt -l 标记（CRLF 行尾，gofmt -d 零差异，非本任务文件；本任务新建文件已 gofmt -w 规整）。creds 参数仅为 ExecutionChannel 接口对齐（kubeconfig 形态校验+用后 Zeroize），连接材料经 CRDClientProvider 按 clusterName 解密获取。
