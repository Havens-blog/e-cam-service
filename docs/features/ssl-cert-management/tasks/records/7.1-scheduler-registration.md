---
status: "completed"
started: "2026-08-19 05:10"
completed: "2026-08-19 05:50"
time_spent: "~40m"
---

# Task Record: 7.1 9 个定时任务注册与 ioc/cert wire 装配（scan/probe/inspection/window-expiry/pause-timeout/orphan-cleanup/crd-recheck/executing-timeout/scan-timeout 挂 internal/task 框架）

## Summary
9 类定时任务调度注册 + cert 域 ioc/wire 全量装配。新增 internal/cert/scheduler/jobs.go：CertJobs 窄端口依赖集（8 个编译期断言对齐 3.5/4.4/5.1/5.7/5.9/5.10 既有服务）+ JobSpecs 构建 8 个调度点承载 9 类任务（映射见包注释：probe 天级由 cert:inspection 第三步承载不重复注册避免同日双重全网拨测；probe 提频+window-expiry 由 cert:verify-window 周期=verifyProbeIntervalMinutes 扫描式承载，与 5.10 实现形态一致；该调度点终局后逐单 ConsumeOrderQueue=orphan-cleanup 事件触发路径）；频率对表：scan 天级 02:00、scan-timeout */15、inspection 天级 05:00、pause-timeout 每小时、orphan-sweep 天级 03:00、crd-recheck 分钟级（5.9 扫描式延迟消费，分钟级扫描保证 recheckDelayMinutes=5min 延迟契约——修正 5.summary 提议的天级口径）、executing-timeout */5；每入口 guarded 包装（单飞互斥防 ecron goroutine 重叠 + panic recover 不击穿调度进程）；scan 调度侧 ErrScanInProgress 静默让位（与 scan-timeout 恢复联动）；pause-timeout 超时取消清单逐单经 ops 类 CertAlertEvent 发布处置通知。新增 internal/cert/module.go：InitCertModule 量装配（12 仓储 + 18 服务 + CloudAPIChannel 双云部署器注册 + K8sAPIChannel + 五云扫描适配 + K8s 扫描网关 + 账号源/凭证源 + InspectionJob + 6 handler），EnsureIndexes 与 NewEnvelopeCryptoFromEnv fail-fast；queue 可用时 TaskxItemDispatcher+ChangeItemExecutor 挂既有 cam 任务队列（与 RegisterBillingExecutor 同机制），nil 时 unavailableDispatcher 显式降级不阻断 boot；VerifyProbeIntervalMinutes 装配期从 AlertConfig.thresholds 解析存 Module 字段（AC-6，运行期改动需重启）；scan-timeout 通知经 publisherScanNotifier 适配 4.3 发布器；recorder/auditor/unmet/orphan/audit 等消费端口按 5.summary 装配边界约定 nil 降级（报告存档/审计归 7.2）。ioc：新增 ioc/cert.go（InitCertModule provider：云账号/资产/任务队列复用 cam 既有装配 + CertAlertPublisher 生产装配=alert.NewCertAlertPublisher+LoadCertSMTPConfig，依赖方向 alert→cert 由组合根构造后注入；initCertJobs 构建 ecron 组件）；InitJobs 扩签名 (camModule, certModule) 既有 cam 条目零改动仅 append cert 项；InitWebServer 扩 certModule 参数挂载 /api/v1/certs 路由；App 增 CertModule；wire_gen.go 经 wire v0.7.0 重新生成（InitCertModule 于 camModule 之后、WebServer/Jobs 之前注入）。附带修复 3.5 潜在缺陷：ScanSnapshotRepository.FinishScan 对 nil coverageMeta $set null 被集合校验器（bsonType=array）拒绝——空归一为 []（与 partialFailures 同防御，7.1 冒烟实测）；execute_service.go ChangeExecuteService 接口 additive 增列 ExecuteItem（实现已存在，使模块装配可凭接口值构造 ChangeItemExecutor）。验证：go build ./... PASS；scheduler 包 92.3% / cert 包 82.0%；mongox 实例冒烟 TestInitCertModule_BootSmoke（全量装配无错+手动 scan 空范围优雅失败+手动 inspection 全流水线+路由挂载+9 调度点全执行无 panic）；cam/alert/audit/task 回归全绿；全仓其余 20 测试包全绿。

## Changes

### Files Created
- internal/cert/scheduler/jobs.go
- internal/cert/scheduler/jobs_test.go
- internal/cert/module.go
- internal/cert/module_test.go
- ioc/cert.go

### Files Modified
- ioc/jobs.go
- ioc/gin.go
- ioc/app.go
- ioc/wire.go
- ioc/wire_gen.go
- internal/cert/service/execute_service.go
- internal/cert/repository/scan_snapshot.go

### Key Decisions
- 9 任务→8 调度点映射：probe 天级折叠进 cert:inspection（4.4 InspectionJob 第三步即探测调度，独立注册会同日双重全网拨测）；probe 提频+window-expiry 合一 cert:verify-window（5.10 为扫描式承载，统一扫描活跃窗口而非按订单登记定时器——实现取舍与 5.10 一致记录于注释）
- crd-recheck 分钟级而非 5.summary 提议的天级：tech-design 延迟契约=patch 后 recheckDelayMinutes（默认 5 分钟），到期扫描式消费需分钟级扫描保证延迟语义（SPEC 权威裁决）
- verifyProbeIntervalMinutes 装配期读取（DB 单文档）：ecron 静态 cron 表达式承载，运行期改动需重启生效（记录于 Module 字段注释与 ioc 注释）；非整除 60 的取 minute%n==0 集合，小时边界间隔不均（cron 表达能力限制，界值 5~60 默认 10 整除）
- ChangeItemExecutor 挂 cam TaskModule 既有队列而非新建 internal/task 队列：internal/task 模块未入 wire 图，cam 任务队列是进程内唯一 taskx 实例（RegisterBillingExecutor 同机制先例）；ChangeExecuteService 接口 additive 增列 ExecuteItem 使接口值可直构执行器（无 fake 受影响）
- InspectionRunStore 保留内存实现：4.4 留缝称 7.1 '可换'持久化，但 schema.sql 无巡检运行集合且 7.1 AC 未列——新增集合属存储接线（7.2/后续），Job 侧零改动可替换；同口径 VerifyWindowRecorder/RollbackAuditRecorder/OrphanCleanupRecorder/ChangeAuditSource nil 降级（5.summary 装配边界：报告存档/审计归 7.2，5.11 查询侧已有同口径推导降级）
- 全进程 app boot（InitApp）本机不可行（无本地 etcd/redis/ldap）：以 wire 图编译期验证（wire 生成成功+全量 build）+ mongox 实例模块级冒烟（全链路装配/手动触发/路由/调度点执行）+ ecron 表达式可解析测试近似 AC-5，环境限制如实记录

## Test Results
- **Tests Executed**: Yes
- **Passed**: 13
- **Failed**: 0
- **Coverage**: 82.0%

## Acceptance Criteria
- [x] 9 个任务全部注册，频率/触发按 Scheduler Tasks 表：scan 天级+手动(防重 409)；scan-timeout 每 15 分钟；probe 天级+窗口内提频；inspection 天级；window-expiry 周期=verifyProbeIntervalMinutes；pause-timeout 每小时；orphan-cleanup 天级批扫+事件触发；crd-recheck 延迟 recheckDelayMinutes；executing-timeout 每 5 分钟
- [x] internal/cert/module.go + ioc/cert.go 注入：repository/service/deployer/scheduler/web 全量 wire；wire_gen.go 重新生成且 app 启动注入 cert 域无报错
- [x] 任务入口幂等+并发安全：同一任务不重复并发执行；scan/probe/inspection 失败不阻塞其他任务；scan 防重与 scan-timeout 恢复联动
- [x] 既有 internal/cam 与其他域任务注册不受影响（不改动既有 task 注册项；新增项独立）
- [x] 启动冒烟：app boot 后任务按频率进入排队；manually 触发 scan/inspection 各一次无 panic（mongox test 或开发库）
- [x] 频率/阈值参数从 AlertConfig.thresholds 读取（接调度周期参数注入）

## Notes
测试：13 个新增用例全过（scheduler/jobs_test.go 10 个：频率对表/窗口表达式界值/AC-6 阈值解析/scan 防重让位/窗口事件触发孤儿消费/暂停超时通知/panic 恢复/单飞互斥/全调度点可执行/cron 表达式可解析；module_test.go 3 个：mongox 实例 boot 冒烟[DSN-gated]/无队列降级装配/空路由防御）；scheduler 包覆盖率 92.3%（jobs.go 新逻辑）、cert 包 82.0%（module.go InitCertModule 88.4%）；coverage 字段取 cert 包（主装配交付物），scheduler 包见本注。mongox DSN 实例（config/prod.yaml 指向）跑通冒烟与 repository/service/web 全套；既有失败仅记忆已录两个（TestCertificateRepo_ListPage 夹具缺陷/TestCertificateRepo_GetByIDAndAttachPrivateKey 远端读己之写延迟）+远端偶发 socket EOF（单测重跑即过），均非本任务代码。门禁：go build ./... PASS；go vet（cert+ioc）PASS；gofmt 新建文件 clean，scan_snapshot.go 检出为既有 CRLF 噪声（tr -d '\r' 后 delta=0，未触碰行尾）。-race 不可用（宿主无 cgo，仓内既有口径）。U+FFFD 扫描修复 5 处（module.go/jobs.go×2/jobs_test.go/execute_service.go——含 1 处 5.7 既有坏字'连'）。wire CLI 经 go install github.com/google/wire/cmd/wire@v0.7.0 全局安装后生成（go run 形态因 go.sum 缺 cmd 依赖不可用，未改动 go.mod/go.sum）。robfig/cron/v3 保持 // indirect（仅测试导入，未跑 go mod tidy 防共享 go.sum 大面积扰动）。7.2 待接线遗留：VerifyWindowRecorder/RollbackAuditRecorder/OrphanCleanupRecorder 存档、ChangeAuditSource/UnmetSource/OrphanCleanupSource 生产存储、EIAM CtxRoleKey 接线、ExecuteAlertNotifier 恢复通知、InspectionRunStore 持久化（schema 增集合）、ManagementSignalConfig/退避参数应用 config 注入面。按 dispatcher 指示：未提交 git。
