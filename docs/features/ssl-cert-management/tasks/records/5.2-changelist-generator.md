---
status: "completed"
started: "2026-08-19 00:08"
completed: "2026-08-19 00:27"
time_spent: "~19m"
---

# Task Record: 5.2 清单生成（指纹聚合、4 项前置校验含 NEW_CERT_FINGERPRINT_ONLY、SAN 预检、不可执行项分区、盲区声明、快照绑定）

## Summary
清单生成 GenerateChangeList：按旧证书指纹聚合最新成功快照 CertReference（资源去重），逐项生成 ChangeListItem（resourceRef 按 action 分支持久化完整 DeployTarget：cloud_api={channel,cloud,product,accountKey,resourceId} / patch_crd={channel,clusterId,namespace,kind,resourceId}）；四项前置校验全部前置于生成期并映射 409（SCAN_STALE 含无成功快照、CHANGE_IN_FLIGHT 含插入竞态索引兜底、NEW_CERT_FINGERPRINT_ONLY、SAN_INSUFFICIENT 基准=旧证书 SAN）；SAN 预检结果（Passed/Missing/NewSANs，大小写不敏感）；不可执行项分区（discovery-only 三云 ERR_DISCOVERY_ONLY、K8s 经注入的 ManagementProbe 三信号判定——5.6 前未接入/探测失败均按不可执行分区不静默放行，持久化即标 skipped 不计执行成功率分母）；盲区声明写入 Warnings（VM Nginx 覆盖边界恒定居首、partialFailures 盲区提示、coverageMeta total=-1 分母不可用、分区汇总出路）；预生成 pending_confirm 订单绑定 snapshotId+activeMutex+ScanFreshnessHrs。

## Changes

### Files Created
- internal/cert/service/changelist_generator.go
- internal/cert/service/changelist_generator_test.go

### Files Modified
- internal/cert/service/change_service.go
- internal/cert/service/change_service_test.go
- internal/cert/domain/errors.go

### Key Decisions
- K8s 管理权三信号经 ManagementProbe 接口注入解耦（实现属 5.6）：探测通道未注入或探测失败时 K8s 项按不可执行项分区并显式声明原因，不静默放行也不阻断清单生成（单点失败不阻塞其他目标）
- 不可执行项持久化即标 status=skipped + Error=Reason（对齐 ItemStatusSkipped 语义），保证 5.7 执行引擎仅执行 pending 项、不可执行项不计入执行成功率分母
- SAN 预检基准 = 旧证书 SAN（清单项所服务域名并集的可计算来源）；旧证书未登记台账时 ErrNoDocuments 透传（发起更换前提是在册证书），规避空基准静默放行
- 聚合源 = ListBySnapshotID(最新 done 快照) 后按指纹过滤 + resourceDedupKey 资源去重（与覆盖率 covered 同键），跨快照/异指纹引用不混入
- 无成功快照同样映射 SCAN_STALE（清单强制绑定最近扫描，新鲜度无从建立即阻断）；变更项 ID 服务层预生成以保证清单项与持久化项一一对应（仓储 CreateMulti 不回填 _id）
- CHANGE_IN_FLIGHT 复用仓储哨兵 domain.ErrChangeInFlight（uk_active_mutex 索引强制），新增三个 CertError 哨兵（ErrScanStale/ErrSanInsufficient/ErrNewCertFingerprintOnly）供 web 层映射 409

## Test Results
- **Tests Executed**: Yes
- **Passed**: 28
- **Failed**: 0
- **Coverage**: 79.2%

## Acceptance Criteria
- [x] GenerateChangeList 聚合最新成功快照该指纹 CertReference，逐项生成 ChangeListItem（resourceRef 按 action 分支必填：cloud_api={channel,cloud,product,accountKey,resourceId}；patch_crd={channel,clusterId,namespace,kind,resourceId}）
- [x] 四项前置校验阻断→409：SCAN_STALE（now-startedAt>scanFreshnessHours，含无成功快照）/SAN_INSUFFICIENT（Missing 非空）/CHANGE_IN_FLIGHT（activeMutex，含插入竞态）/NEW_CERT_FINGERPRINT_ONLY（hostingStatus≠complete）
- [x] 不可执行项分区：discovery-only 云（huawei/aws/azure）AutoChangeable=false+Reason；K8s 三信号经 managementProbe 接口注入（5.6 实现），未接入/失败不静默放行；不可执行项不计入执行成功率分母（持久化即 skipped）
- [x] 盲区声明写入 Warnings：VM Nginx 覆盖边界、blind_spot（partialFailures）项提示、coverageMeta total=-1 分母不可用提示
- [x] ChangeList 持久化预生成订单（pending_confirm）+绑定 snapshotId+ScanFreshnessHrs；SAN 预检结果（Passed/Missing/NewSANs）
- [x] 单元测试：四阻断各一次命中、SAN 预检 Missing/通过、不可执行项分区与 Warnings、resourceRef 分支必填字段

## Notes
TDD：stub RED（全部 not implemented）→ 实现 GREEN。验证：go build ./... ok；go vet ./... ok（golangci-lint 未安装跳过）；gofmt -s 清洁（本任务新文件；仓内既有文件存在 CRLF/注释风格 gofmt 偏差，非本任务改动范围）；go test ./internal/cert/... 全部 7 包 ok。覆盖率：service 包 79.2%（runner 报告），本任务新增 changelist_generator.go 函数级 85.7%~100%（GenerateChangeList/buildChangeItems/assessChangeable/listWarnings/normalizeDomain 100%）。ChangeService 构造器扩展（+snapshots/refs/probe）已同步 5.1 既有测试 14 处调用点，全部保持绿。
