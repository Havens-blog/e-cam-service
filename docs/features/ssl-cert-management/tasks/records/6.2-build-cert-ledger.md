---
status: "completed"
started: "2026-08-19 03:31"
completed: "2026-08-19 03:48"
time_spent: "~17m"
---

# Task Record: 6.2 台账组件构建（UF-1：统计卡/表格/导入 Modal/批量导入/删除拦截/补传私钥，含 loading/empty/error 态）

## Summary
e-cam-web 证书台账页（UF-1）全部组件落地：index.vue 四态页面（loading 骨架 3 卡+5 行/800ms 延迟显示、empty 引导批量导入、error 重试、populated）+ StatsCards（完整托管/仅指纹+占比/登记率+可更换托管双口径，GET /certs/stats）+ CertTable（域名/SAN[指纹 mono 首8…末8+复制]、签发者、有效期、剩余四段状态色徽章、托管状态、保护期锁徽章、引用数、操作[详情/发起更换(完整托管)/⋯补传私钥(仅指纹)/删除]；搜索 300ms 防抖+托管状态/剩余天数筛选+服务端分页 20/页，SAN>3 折叠+N，行点击进详情）+ ImportCertModal（PEM 拖拽+私钥可选+预期域名提示性 Warning 不拦截；阶段化合并 loading 上传中/解析中/校验中，终态前 Modal 全禁不可关闭；四类 CERT_* 校验错误横幅逐项展示）+ BatchImportModal（720px 多文件/拖拽+逐文件可选私钥[去扩展名基名配对]+zip 解包[fflate]；2s 轮询会话至终态；处理中进度条+计数+关闭二次确认「终止导入」；逐文件结果+失败行单文件重试；完成 Toast N成功/M失败；localStorage batchId 中断重入恢复）+ UploadKeyModal（匹配校验通过即时升级完整托管，CERT_KEY_MISMATCH 横幅重试）+ 删除拦截/二次确认双 Modal（行级预判 canDeleteRow + 服务端 409 CERT_HAS_REFS 结构化 meta 双保险）。纯逻辑抽取 src/views/cert/ledger/format.ts（20 个纯函数）node 环境单测 39 例；cert.ts 为删除拦截结构化原因增加 CertRequestError.meta 透传（向后兼容）。跨仓库：任务文件在 e-cam-service，代码位于 D:\Haven\e-cam-web。

## Changes

### Files Created
- e-cam-web/src/views/cert/ledger/format.ts
- e-cam-web/src/views/cert/ledger/format.test.ts
- e-cam-web/src/views/cert/ledger/components/StatsCards.vue
- e-cam-web/src/views/cert/ledger/components/CertTable.vue
- e-cam-web/src/views/cert/ledger/components/ImportCertModal.vue
- e-cam-web/src/views/cert/ledger/components/BatchImportModal.vue
- e-cam-web/src/views/cert/ledger/components/UploadKeyModal.vue

### Files Modified
- e-cam-web/src/views/cert/ledger/index.vue
- e-cam-web/src/api/cert.ts
- e-cam-web/src/api/cert.test.ts
- e-cam-web/package.json
- e-cam-web/pnpm-lock.yaml

### Key Decisions
- SPEC CONTRADICTION（搜索/筛选执行位置）：ui-design Data Binding 称「过滤在当前页数据上即时执行」，但任务 AC 与 api-handbook GET /certs 契约提供服务端 search/hostingStatus/daysLeft 参数且要求服务端分页——按更具体/更新的 AC+API 契约走服务端过滤（页码重置 1，输入 300ms 防抖保留即时体感）
- 「新证书置顶」按 API 可达语义实现：列表服务端排序为 notAfter asc（repository/certificate.go，到期优先的产品排序不可由前端改写），导入成功后刷新+新证书行 accent 高亮 5s+Toast 附指纹，替代字面置顶
- 删除拦截双保险：行级 canDeleteRow(refCount/protectUntil) 预判直接出拦截 Modal（无删除按钮）；可删行走红色二次确认→DELETE；服务端 409 CERT_HAS_REFS（含 blind_spot 行级不可见原因）经 CertRequestError.meta（后端 deleteBlockedMeta{referenceStatus,refCount,reason,protectUntil}）渲染结构化原因——为此对 6.1 的 cert.ts 做最小增强（meta 可选透传，不破坏既有签名/测试）
- 预期域名比对为客户端实现：api-handbook ImportCertResult 无 warning 字段，服务端仅返回四类错误；实现 extractPemDomains（base64 解 DER 后扫描 IA5 ASCII 域名串，CN/SAN 均原文出现，快速路径不引 ASN.1 依赖）+ expectedDomainCovered（精确/通配符覆盖），仅产生表单内 Warning，不拦截提交——与 PRD「提示性比对」一致
- zip 解包为客户端实现（后端无 zip 处理）：新增 fflate@0.8.3 直接依赖（原仅 xlsx 传递依赖不可直接 import），unzipSync+splitZipEntries 按 .key 扩展名/.crt 扩展名优先+PEM 内容嗅探（PRIVATE KEY 头）分流证书/私钥，同名基名自动配对；逐文件附加的私钥提交时重命名为 `${去扩展名基名}.key` 与后端配对规则对齐
- 批量导入单文件重试按 api-handbook 语义=重新 POST 单文件生成新会话，取 files[0] 就地合并回当前会话展示（计数本地重算）；中断重入经 localStorage lastBatchId 恢复（running 续轮询/终态展示结果；恢复会话失败行无 File 句柄→提供重选文件后重试）
- vitest 为 node 环境纯逻辑测试（仓库无 DOM 测试环境与 jsdom 依赖），沿用 6.1 模式将展示逻辑抽取为纯模块 format.ts 单测；组件层以 vue-tsc 模板类型检查+vite build 验证
- 测试环境覆盖声明：coverage 记 null（vitest.config 未接 coverage provider，同 6.1 口径）；可测逻辑层（format.ts 全部 20 函数+信封 meta 透传）均有直接单测

## Test Results
- **Tests Executed**: Yes
- **Passed**: 88
- **Failed**: 0
- **Coverage**: 0.0%

## Acceptance Criteria
- [x] 统计卡行（3 列）：完整托管数 / 仅指纹登记数（含占比）/ 覆盖率（登记率+可更换托管率），数据绑定 GET /api/v1/certs/stats
- [x] 表格：列（域名/SAN·签发者·有效期·剩余[状态色]·托管状态·保护期[锁徽章]·引用数·操作）；搜索+托管状态/剩余天数筛选+服务端分页（20/页）；行点击进详情；SAN chips 超 3 个折叠+N；指纹类 mono 截断（首8…末8）+复制
- [x] 导入 Modal：PEM+私钥(可选)+预期域名(可选)；提交合并 loading（按钮 disabled+spinner+阶段文案，不可关闭）；校验失败表单顶部 Error 横幅逐项列错误（四类 CERT_*）；expectedDomain 不一致仅 Warning 提示不拦截；成功关闭+Toast+列表刷新置顶
- [x] 批量导入 Modal（720px）：多文件/拖拽+逐文件可选私钥+zip 解包；处理中进度条+计数+不得中途关闭（关闭需二次确认）；逐文件结果列表（成功[完整/仅指纹]/失败+原因）+失败行单文件重试；完成 Toast N成功/M失败；中断重入展示上次会话结果（batchId 轮询恢复）
- [x] 删除与补传私钥：有活跃引用/保护期 → 拦截 Modal 仅说明原因（N 个引用/保护期至 X 日）无删除按钮；可删 → 红色后果二次确认后删除+Toast；仅指纹行"补传私钥"→ 匹配校验通过即时升级完整托管，失败展示 CERT_KEY_MISMATCH
- [x] 四态齐备：loading 骨架（5 行+3 卡）/empty（空态引导批量导入）/error（重试）/populated；a11y：label 关联、focus ring、错误 aria-describedby

## Notes
验证：vitest 全量 88 通过（新增 40：format.ts 39 + cert.ts 信封 meta 透传 1）；npm run build:check（vue-tsc -b && vite build）exit 0；eslint 新增/修改文件零违规。Hard Rules 复核：任何界面不展示明文私钥（组件仅持 File 句柄，UploadKeyModal 不读内容渲染）；导入终态前 Modal 不可关闭+按钮 disabled（show-close/点遮罩/Esc/取消全禁）。新建依赖 fflate@0.8.3（zip 解包，AC4 要求，后端无 zip 处理）。未提交 git（按任务指示 Do NOT commit）。遗留提示：恢复会话的失败行重试需重选文件（浏览器不持久化 File 句柄），已按 ui-design「未处理文件需重新选择上传」语义实现 UI 提示。
