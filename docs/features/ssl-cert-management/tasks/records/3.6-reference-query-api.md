---
status: "completed"
started: "2026-08-18 20:19"
completed: "2026-08-18 20:29"
time_spent: "~10m"
---

# Task Record: 3.6 引用查询 API（正向分组+覆盖率、反向查询、立即扫描防重 409 SCAN_IN_PROGRESS）

## Summary
Implemented reference query API: forward grouped reference view (GET /:id/references with cloud/product/cluster grouping, lastScanAt + coverage metadata carrying the total=-1 '分母不可用' marker, referenceStatus three-state derivation, and the mandatory coverage boundary declaration), reverse query (GET /reverse?domain= matching SAN/CN with single-label wildcard coverage and exact resourceId, entries strictly separated by fingerprint including unregistered fingerprints, empty list on no match), and immediate scan trigger (POST /:id/scan delegating to 3.5 StartScan with 409 SCAN_IN_PROGRESS duplicate prevention carrying in-progress snapshot info). Extracted the 2.3 referenceStatus derivation into shared deriveRefStatusFor to avoid duplication.

## Changes

### Files Created
- internal/cert/service/reference_query_service.go
- internal/cert/web/reference_handler.go
- internal/cert/web/reference_handler_test.go

### Files Modified
- internal/cert/service/ledger_service.go
- internal/cert/web/response.go
- internal/cert/web/router.go
- internal/cert/web/cert_handler_test.go
- internal/cert/web/ledger_handler_test.go

### Key Decisions
- Extracted referenceStatus derivation core into shared package-level deriveRefStatusFor (lazy history fetch preserved) instead of copying ledger logic; ledger method now delegates
- ReferenceQueryService depends on narrow ScanTriggerPort (satisfied by 3.5 ReferenceScanService.StartScan) rather than the full scan service interface
- ScanInProgressError wraps domain.ErrScanInProgress and carries running snapshot info for the 409 meta envelope; response.go gained the SCAN_IN_PROGRESS 409 mapping as generic fallback
- Reverse query candidates keyed strictly by fingerprint map (Hard Rule: no same-domain merging); wildcard SAN covers single-label subdomains only (a.b.c does not match *.c)
- Group ordering: cloud groups lexicographic first, K8s cluster groups (empty cloud) last; groups and references sorted deterministically
- Coverage VO emits denominatorAvailable=false plus '分母不可用（覆盖率盲区）' note when total=-1 instead of showing 0%

## Test Results
- **Tests Executed**: Yes
- **Passed**: 16
- **Failed**: 0
- **Coverage**: 89.1%

## Acceptance Criteria
- [x] GET /api/v1/certs/:id/references: cloud/product/cluster grouped references (resourceId/referencedCloudCertId/accountKey/namespace/kind) + lastScanAt + coverage with total=-1 '分母不可用' marker + referenceStatus + coverage boundary declaration
- [x] referenceStatus=no_refs_scanned clearly distinguished from blind_spot (no scan), with last scan time attached
- [x] GET /api/v1/certs/reverse?domain=: domain/resource reverse lookup, fingerprint-separated entries without same-domain merging, no match returns empty list not error
- [x] POST /api/v1/certs/:id/scan triggers 3.5 scan; in-progress returns 409 SCAN_IN_PROGRESS with in-progress snapshot info
- [x] httptest contract tests: three-branch referenceStatus, reverse fingerprint separation, 409 duplicate prevention, error envelope format

## Notes
Hard Rules enforced and asserted in all three forward-view branches: coverageBoundary carries '本视图不含 VM Nginx 配置级引用' and can never be omitted; reverse entries are keyed per fingerprint. Static checks: go build ./... clean, gofmt clean, go vet clean (golangci-lint not installed in env; go vet used as lint fallback). Coverage 89.1% is the web package runner output (target 80%); new service file functions measured 82.8-100% via cross-package coverage. Both Hard Rules and AC5 subtests pass; full internal/cert suite green (domain 95.1%, k8s 97.4%, web 89.1%).
