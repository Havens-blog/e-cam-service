---
status: "completed"
started: "2026-08-19 03:49"
completed: "2026-08-19 04:02"
time_spent: "~13m"
---

# Task Record: 6.3 详情与引用关系组件（UF-2：要素卡/正反向 Tab/盲区横幅/扫描防重/筛选）

## Summary
e-cam-web 证书详情与引用关系页（UF-2）全部组件落地：index.vue（面包屑/页面头/要素+引用两卡独立加载/路由参数切换重载/只读模式）+ ElementCard（SAN chips >3 折叠+N/issuer/指纹 mono 截断+复制/托管徽章/有效期进度条状态色+已消耗%/私钥已加密托管/关联变更历史按当前证书过滤倒序）+ ReferenceCard（正向|反向 Tab、引用视图独立错误重试、立即扫描防重状态机：进行中 disabled+扫描中+5s 轮询 lastScanAt>=startedAt 完成刷新、sessionStorage 会话重入/刷新恢复轮询、409 SCAN_IN_PROGRESS 解析 meta 不新建任务）+ ForwardReferences（盲区横幅常驻不可关闭 Hard Rule/扫描元数据行 relativeTime+各云覆盖率 total=-1 分母不可用+超期 Warning/云产品集群级联筛选集群仅 K8s 组/资源名 300ms 防抖/分组折叠默认前 2 组/无匹配引用空态/未发现引用三态文案/行点击 Enter Space 开抽屉）+ ReverseQuery（域名资源查询按指纹逐卡严格区分不合并 Hard Rule/未查询到区别于初始态）+ ResourceDrawer（480px 右抽屉要素原文）。纯展示逻辑沉 detail/format.ts（16 导出函数）node 单测 50 例全绿。

## Changes

### Files Created
- e-cam-web/src/views/cert/detail/format.ts
- e-cam-web/src/views/cert/detail/format.test.ts
- e-cam-web/src/views/cert/detail/index.vue
- e-cam-web/src/views/cert/detail/components/ElementCard.vue
- e-cam-web/src/views/cert/detail/components/ReferenceCard.vue
- e-cam-web/src/views/cert/detail/components/ForwardReferences.vue
- e-cam-web/src/views/cert/detail/components/ReverseQuery.vue
- e-cam-web/src/views/cert/detail/components/ResourceDrawer.vue

### Files Modified
- e-cam-web/src/api/cert.ts

### Key Decisions
- reverseLookupCertsApi 响应类型按后端 ReverseResultVO 契约修正为 {query,count,items} 包装（原 6.1 声明为裸数组，运行时 items 不可达），ReverseLookupCert 补 referenceCount、TriggerScanResult 补终态字段 failReason/referencesWritten
- 扫描轮询无专用状态端点（POST /:id/scan 同步至终态返回）：以 references.lastScanAt（服务端口径=快照 startedAt）>= 会话 startedAt 为完成判据，sessionStorage 按证书隔离持久化进行中会话实现重入/刷新恢复，30min 上限自动放弃防永久扫描中
- 关联变更历史复用 GET /certs/changes（无 certId 服务端过滤参数）客户端过滤：旧证书指纹或新证书 ID 命中即关联 + createdAt 倒序；只读角色该接口 403 故整块隐藏
- 立即扫描按钮按原型 default 态常显（非只读），超期（客户端 24h 常量，settings 仅主管可读）仅元数据行变 Warning 强调
- 盲区横幅静态渲染于正向 Tab 顶部队所有引用状态分支均存在（Hard Rule），role=note 无关闭钮

## Test Results
- **Tests Executed**: Yes
- **Passed**: 138
- **Failed**: 0
- **Coverage**: 0.0%

## Acceptance Criteria
- [x] 要素卡：左 SAN chips（折叠+N）/issuer/指纹(mono 可复制)/托管状态徽章；右 有效期进度条(剩余天数状态色)/私钥已加密托管/关联变更历史列表（按 certId 过滤倒序）
- [x] 引用卡：正向|反向 Tab；正向盲区横幅常驻不可关闭；扫描元数据行（最近扫描时间+各云覆盖率，total=-1 显示分母不可用���+ 立即扫描按钮（超期时显示）
- [x] 立即扫描防重：进行中按钮 disabled+扫描中+轮询任务，完成后刷新元数据；重入/刷新直接恢复扫描中态轮询；服务端 409 SCAN_IN_PROGRESS 不新建任务、返回进行中态
- [x] 正向分组折叠（cloud·product），筛选 云▾/产品▾/集群▾级联（集群仅 K8s 组显示）+ 资源名搜索 300ms 防抖（清空恢复全量）；无匹配分组显示无匹配引用空态；资源行点击 → 右抽屉展示资源要素
- [x] 反向查询：域名/资源名搜索 → 证书卡片列表（按指纹分组并存区分）；无匹配 → 未查询到引用该域名/资源的证书（区别于未查询的初始态）
- [x] 未发现引用态显示未发现引用（≠ 无引用）· 最近扫描 X 前（区别于空态）；只读模式隐藏立即扫描等操作入口；a11y 焦点陷阱/aria-live 合规

## Notes
验证：npx vitest run 138/138（含本任务新增 detail/format.test.ts 50 例）；npm run build:check（vue-tsc -b + vite build）exit 0；eslint detail 目录+cert.ts 无告警。测试环境覆盖声明：coverage 记 null（vitest.config 未接 coverage provider，同 6.1/6.2 口径）；可测逻辑层（format.ts 16 函数）均有直接单测。跨仓库：文件位于 e-cam-web（未提交，按任务指令）。
