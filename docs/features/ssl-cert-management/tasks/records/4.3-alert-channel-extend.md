---
status: "completed"
started: "2026-08-18 22:17"
completed: "2026-08-18 22:31"
time_spent: "~14m"
---

# Task Record: 4.3 告警通道扩展（internal/alert 复用扩展 webhook+email、verifyWindowRoute 窗口路由、发送失败退避）

## Summary
证书域告警通道扩展：internal/alert 新增 CertAlertPublisher（实现 4.2 certservice.CertAlertPublisher 缝）——webhook POST JSON（白名单 payload，含 category/orderId/摘要/SAN 截断）+ email（复用既有 EmailSender，SMTP 凭据经应用 config alert.cert_smtp 注入、缺省启动警告不失败）；RouteCertAlert 纯函数路由判定（change_linked 窗口开启+verifyWindowRoute.enabled=true 走专用通道附 orderId/预期指纹/达标计数；enabled=false 复用常规通道+changeLinked 标记；VerifyWindow.Active=false 恢复常规路由，5.10 经事件入参控制）；固定退避序列（1+4 次有界重试，成功 URL 不重复投递，ctx 取消即中止）；终态投递记录直写 ecam_alert_event（sent/failed+原因，不入 pending 队列避免既有 ProcessPendingEvents 干扰）；新增 AlertCategoryOps 运维处置类标记与 VerifyWindowContext 路由上下文（cert/service additive）。既有 alert 文件零修改。

## Changes

### Files Created
- internal/alert/channel/certwebhook.go
- internal/alert/channel/certwebhook_test.go
- internal/alert/service/cert_alert_publisher.go
- internal/alert/service/cert_alert_publisher_test.go
- internal/cert/service/alert_channel_ext.go

### Files Modified
- internal/cert/service/alert_events.go

### Key Decisions
- 依赖方向 internal/alert → internal/cert（cert 不反向 import alert，无环）；ioc 装配留给 7.1（cert 模块尚无 ioc 文件），本任务提供 LoadCertSMTPConfig() 使装配零成本
- 投递记录写终态（sent/failed）而非 pending：既有 GetPendingEvents 按 status=pending ���差别拾取，pending 记录会被旧处理器误消费
- webhook 发送器自带 POST 而非复用 postJSON：*url.Error 携带完整 URL（常内嵌 token），网络错误仅保留内层错误、状态错误仅保留状态码（Hard Rule 凭证不外泄）
- 无接收人/仅邮件但 SMTP 未配置为配置态：留存 failed 记录返回 nil（不触发上轮 at-least-once 重发）；有接收人最终投递失败返回 error（4.2 at-least-once 语义）
- CertAlertEvent 增量加 VerifyWindow 字段（唯一既有文件改动）；VerifyWindowContext/AlertCategoryOps 放新文件同包定义

## Test Results
- **Tests Executed**: Yes
- **Passed**: 28
- **Failed**: 0
- **Coverage**: 95.0%

## Acceptance Criteria
- [x] 证书域告警消费 4.2 CertAlertEvent：webhook（POST JSON，含 category/orderId/摘要）与 email 两组接收人按 AlertConfig 配置触达；运维处置类通知可标记为非业务四类
- [x] verifyWindowRoute：enabled=true 时 change_linked 事件走专用 webhookUrls/emailGroup（附 orderId、预期指纹、达标计数）；enabled=false 走常规通道但附变更关联标记；窗口关闭后恢复常规路由（路由判定入参化，5.10 控制）
- [x] 发送失败退避：固定退避序列有界重试，最终失败记录失败状态与原因，事件不丢失（ecam_alert_event 终态记录供补发/查询）
- [x] Additive：go build ./internal/alert/... 与既有 alert 测试全绿；既有告警消费方行为不变
- [x] 测试：mock HTTP server（webhook 成功/失败/限流 429）+ mock SMTP（net 监听伪 SMTP）；路由两分支+窗口关闭回退；退避上限后不再无限重试

## Notes
Hard Rules 落实：payload 白名单为固定结构体（无 map 透传，测试断言实际 JSON 键集 ⊆ 白名单）；退避序列固定 4 档=最多 5 次尝试，测试断言请求计数恰为 5 后停止；webhook URL/凭证不进入 payload、邮件正文、错误信息与投递记录（不可达场景测试断言 token 不泄露）。验证命令：go build ./... 通过；go test -count=1 ./internal/alert/... ./internal/cert/... 全绿（alert service/channel 28 用例，新增代码 14 函数覆盖率 95%）。coverage 95.0 为新增源文件（certwebhook.go + cert_alert_publisher.go）函数级聚合实测值。
