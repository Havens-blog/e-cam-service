---
status: "completed"
started: "2026-08-18 22:32"
completed: "2026-08-18 22:50"
time_spent: "~18m"
---

# Task Record: 4.4 定时巡检任务（完整性复检+到期分级+TLS 探测调度+豁免过滤，天级）

## Summary
天级巡检流水线 Job（internal/cert/scheduler）：RunInspection(ctx) 顺序执行四步——完整性复检（复跑 2.1 ParseCertAndKey，nil keyPEM 不解密私钥，校验可解析/未过期/链完整+存储指纹一致性，异常项发 AlertCategoryOps 运维处置事件不阻塞后续）→ 到期分级去重告警（编排 4.2 InspectLedger）→ TLS 探测调度（编排 4.1 ProbeLedgerDomains，台账全部 sans）→ 豁免过滤（exempt/unreachable/wildcard_skipped/change_linked_diff 均不告警，仅常规 diff 发 tls_diff 事件）；单飞幂等守卫（并发第二轮 ErrInspectionInProgress、不落运行记录）；lastInspectionAt+各子步成功率指标经 InspectionRunStore 端口记录（内存默认实现即 service.LastInspectionSource，7.1 可换持久化实现零改动接线 dashboard）；每轮结束输出结构化子步指标日志（平台自身监控）。单证/单域失败不中断整轮（错误聚合返回，运行记录无条件落，RecordRun 脱离 ctx 取消）。

## Changes

### Files Created
- internal/cert/scheduler/inspection_job.go
- internal/cert/scheduler/inspection_job_test.go

### Files Modified
无

### Key Decisions
- 完整性复检复用 domain.ParseCertAndKey(certPEM, nil)：不解密私钥（Job 只读约束），覆盖可解析/未过期/链完整，并新增存储 PEM 指纹 vs 台账指纹一致性核对；异常经 4.3 通道以 category=ops 发布（不计业务四类），单证异常不产生步骤级错误
- 豁免过滤步语义：豁免标记由 4.1 探测时落定（exempt 优先于归属判定），本步做告警过滤——仅常规 diff 发 tls_diff（按探测事件逐轮触发、无跨巡检去重，设计口径四类中仅到期分级跨巡检去重）；change_linked_diff 不在常规通道重发（5.10 验证窗口通道所有权）；unreachable/wildcard_skipped 不告警
- lastInspectionAt 存储采用消费侧 InspectionRunStore 端口+进程内 MemoryInspectionRunStore 默认实现（编译期断言实现 4.5 service.LastInspectionSource）：schema.sql 无巡检运行集合且任务文件限定交付 Job 文件，不擅自新增集合；7.1 装配时换持久化实现 Job 零改动
- 幂等：进程内单飞守卫 + 同日重复执行的到期告警去重委托 4.2 expiryAlertLevel 升级去重（经 Job 用真实引擎测试验证）；被拒并发轮不产生运行记录
- 运行记录无条件落（部分失败也记录供监控），RecordRun 用 context.WithoutCancel 保证收尾不丢；子步 Ok=false 仅表示步骤级错误（List 失败/发布失败/引擎错误），单证单域失败体现在 Failed 计数不改变 Ok

## Test Results
- **Tests Executed**: Yes
- **Passed**: 12
- **Failed**: 0
- **Coverage**: 94.7%

## Acceptance Criteria
- [x] Job 入口 RunInspection(ctx)：顺序执行四步——完整性复检、到期分级计算与去重告警、TLS 探测调度（台帐全部 sans）、豁免过滤（豁免域名探测记 exempt 不告警）
- [x] 完整性复检：复跑 2.1 校验存储证书可解析/未过期/链完整；异常项产生异常事件（计入巡检健康，不计入业务四类告警或归入运维处置类）
- [x] 记录 lastInspectionAt（供 dashboard 展示）与各子步成功率指标（供平台自身监控）
- [x] 幂等：重复执行同一日不产生重复到期告警（依赖 4.2 去重）；单证书/单域名失败不中断整轮巡检
- [x] 集成测试：完整一轮巡检（mock 台帐+探测）断言到期告警只发一次、probe 全量域名、lastInspectionAt 更新

## Notes
验证：go build ./... OK；gofmt 干净；go vet ./internal/cert/... OK；go test ./internal/cert/... 全绿（scheduler 12 测试函数，包语句覆盖 94.7%>80% 目标）。Hard Rules 满足：依赖面仅仓储读取/4.1/4.2/CertAlertPublisher（无 deployer/云 SDK/执行通道）、RunInspection 无任何频率参数（天级节奏归 7.1）。注册挂 internal/task 与 wire 装配按任务边界留 7.1。按 dispatcher 指示：未提交。
