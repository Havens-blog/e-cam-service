---
status: "completed"
started: "2026-08-18 21:54"
completed: "2026-08-18 22:17"
time_spent: "~23m"
---

# Task Record: 4.5 到期看板与配置 API（dashboard summary/items、settings CRUD、exemptions、crds 端点、thresholds 界值校验、test 告警）

## Summary
到期看板与全局配置 API：GET /dashboard（summary countsByLevel[5] 互斥分桶+diffAlertCount+exemptCount+wildcardSkippedCount+三 rate 复用 2.3 Stats 口径、items 子域名行含 referencedClouds（K8s 记 k8s）、lastInspectionAt 经 LastInspectionSource 端口（4.4 接线前 null）、全角色）；GET/PUT /settings（五字段组读写，thresholds 界值 domain.ValidateThresholds 服务端校验、越界 400 整体拒绝不部分写入）；POST/DELETE exemptions（operator 认证上下文留痕）；POST/GET/DELETE crds（接 3.4 CrdRegistrationService，重复 409 CRD_DUPLICATE_REGISTRATION、非法 certFieldPath 400、内置项删除 400）；POST settings/test（经 CertAlertPublisher 发布 category=test 测试告警，成功即置 channelConfirmed）；RequireRoles 角色门卫（settings 面限运维主管/审计 403 FORBIDDEN deny-by-default，7.2 EIAM 接线消费 CtxRoleKey）

## Changes

### Files Created
- internal/cert/web/dashboard_handler.go
- internal/cert/web/settings_handler.go
- internal/cert/web/authz.go
- internal/cert/service/dashboard_service.go
- internal/cert/service/settings_service.go
- internal/cert/service/dashboard_service_test.go
- internal/cert/service/settings_service_test.go
- internal/cert/web/dashboard_handler_test.go
- internal/cert/web/settings_handler_test.go

### Files Modified
- internal/cert/domain/alert_config.go
- internal/cert/domain/alert_config_test.go
- internal/cert/domain/repository.go
- internal/cert/repository/probe_result.go
- internal/cert/service/alert_events.go
- internal/cert/service/probe_service_test.go
- internal/cert/certtest/certtest.go
- internal/cert/web/router.go
- internal/cert/web/cert_handler_test.go
- internal/cert/web/ledger_handler_test.go
- internal/cert/web/reference_handler_test.go

### Key Decisions
- countsByLevel[5] 互斥桶序=[gt30,le30,le14,le7,expired]（UI 卡序，floor daysLeft 口径同台账筛选分档）；item.level 复用 DaysLeftTier 标签，证书粒度计数、域名粒度行
- 三个 rate 字段经 LedgerService.Stats 复用（Hard Rule 口径同 2.3 stats，不另算）；referencedClouds=最新成功快照归属证引用云去重，K8s 引用（cloud 空）记 k8s 与前端 chips 对齐
- wildcardSkippedCount 由台账通配符 SAN×wildcardProbeOverrides 现算（探测前也可见、随配置即时收敛）；diffAlertCount 仅计最新探测 status=diff（change_linked_diff/unreachable/exempt/wildcard_skipped 不计）
- 同域名多证书归属 notAfter 最新证书（线上更可能生效），notAfter 相同按指纹字典序稳定
- channelConfirmed 不在 PUT 更新面，仅由 POST settings/test 成功置位（PRD 渠道确认门控）；无接收人配置时 sent=false+原因不发布
- 角色门卫 deny-by-default：CtxRoleKey 由 7.2 EIAM claims 接线写入，4.5 经 SetRole 注入测试；dashboard 全角色不挂门卫（全局认证链承接）
- domain.ValidateThresholds 与 schema.sql minimum/maximum 1:1（修正原 Go 层不校验注释）；ProbeResultRepository 增 LatestPerDomain（$sort+$group+$first 聚合，看板单查询避免逐域 N+1）
- 4.4 未落地：lastInspectionAt 以 LastInspectionSource 端口承载，nil 时输出 null，不抢占 4.4 存储设计

## Test Results
- **Tests Executed**: Yes
- **Passed**: 184
- **Failed**: 0
- **Coverage**: 85.9%

## Acceptance Criteria
- [x] GET /api/v1/certs/dashboard：summary{countsByLevel[5],diffAlertCount,exemptCount,wildcardSkippedCount,registrationRate,replaceableRate,fingerprintOnlyRate}, items[]{domain,daysLeft,level,hostingType,probeStatus,referencedClouds[]}, lastInspectionAt；三个 rate 口径同 2.3 stats；全角色可访问
- [x] GET/PUT /api/v1/certs/settings：五字段组读写；thresholds 越界 → 400（九字段+expiryLevels 全界值）
- [x] POST/DELETE exemptions[/:domain]；POST/GET/DELETE crds[/:id] 接 3.4 service，重复登记 → 409
- [x] POST /api/v1/certs/settings/test：经 4.3 通道发测试告警，返回成功/失败原因
- [x] 鉴权：settings/crds/exemptions/test 限运维主管/审计（403 FORBIDDEN 否则）；dashboard 全角色（含只读）
- [x] httptest：dashboard 口径、thresholds 各界值边界（含越界 400）、crds 重复 409、权限矩阵

## Notes
验证：go build ./... OK；gofmt 干净；go vet ./... OK；go test ./... 40 包全绿（受影响 domain/service/web 三包 184 顶层用例 0 失败）。新增文件语句覆盖 85.9%（service 新文件 89.6%/web 79.7%/domain alert_config 94.1%）。Hard Rules 满足：thresholds 服务端校验且越界整体拒绝（TestSettings_PutInvalidRejectedEntirely 断言无部分写入）；dashboard/settings 响应白名单无私钥/凭据字段。审计写入按任务边界留 7.2（豁免 operator 已持久化留痕）；4.3 通道落地前 publisher 注入缝回退日志实现。
