---
status: "completed"
started: "2026-08-19 02:52"
completed: "2026-08-19 03:10"
time_spent: "~18m"
---

# Task Record: 5.11 变更管理 API（列表/详情/confirm/execute/rollback/progress/audit 按单查询、报告 ChangeReport 全字段）

## Summary
变更管理 HTTP API 全集落地：GET/POST /changes（状态 Tab 筛选 + 生成清单四阻断 409）、GET /:id 详情（ChangeList 结构，终态附 ChangeReport 全字段）、confirm（batchConf 越界 400）/execute/confirm-batch（409 BATCH_NOT_CONFIRMABLE）/cancel（409 CHANGE_NOT_CANCELLABLE）、progress 逐项轮询（itemStates+error+批次）、rollback（仅成功项，无效目标 409 ROLLBACK_TARGET_INVALID 附转人工提示）、audit 按单查询（{at,actor,action,detail,itemId?}，7 类 action 全覆盖）。新增 ChangeQueryService（只读聚合：列表/详情/报告/进度/审计）与 ChangeReport/VerifySummary 等报告类型（GetReport 为 Interface 3 单一聚合入口）；响应信封错误映射扩展七个变更 409 码 + batchConf/回滚范围 400 + 状态冲突 409；ChangeOrderRepository 新增 ListPage 分页原语（mongo 实现 + certtest 假实现 + 集成测试，本机无 Mongo 跳过）。5.8 回滚范围错误改类型化哨兵 ErrRollbackScopeInvalid 供 400 映射（行为不变）。审计/未达标/孤儿清理读取端口（ChangeAuditSource/ChangeUnmetSource/ChangeOrphanCleanupSource）随端点契约交付，生产存储接线归 7.2。

## Changes

### Files Created
- internal/cert/service/change_query_service.go
- internal/cert/web/change_handler.go
- internal/cert/web/change_handler_test.go

### Files Modified
- internal/cert/domain/repository.go
- internal/cert/domain/errors.go
- internal/cert/repository/change_order.go
- internal/cert/repository/change_order_test.go
- internal/cert/certtest/change_fakes.go
- internal/cert/service/rollback_service.go
- internal/cert/service/probe_service_test.go
- internal/cert/web/response.go
- internal/cert/web/router.go
- internal/cert/web/cert_handler_test.go
- internal/cert/web/dashboard_handler_test.go
- internal/cert/web/ledger_handler_test.go
- internal/cert/web/reference_handler_test.go

### Key Decisions
- GetReport 未在任何既有 service 实现：按 tech-design Interface 3 新增 ChangeQueryService 查询端承载（ChangeReport 为查询时聚合的非持久化载荷），handler 保持薄绑定/映射层
- UnmetDomains 权威来源为 5.10 VerifyWindowRecorder 固化存档（partial_completed 防窗口关闭后探测漂移），存档未接线时退化为与 judgeWindow 同口径的查询期推导（通配符 override/连续 verifyConfirmProbes 一致判定）
- 七个变更 409 哨兵同为 *CertError，须在 mapError 的 CertError→400 兜底之前显式拦截（否则误映射 400）；ErrChangeInFlight 为仓储层哨兵，补 domain.CodeChangeInFlight 常量对齐 api-handbook
- 5.8 resolveRollbackScope 错误原为无类型 fmt.Errorf（客户端错→500），最小外科改动包装类型化 ErrRollbackScopeInvalid→400 INVALID_REQUEST，与既有测试无断言冲突
- InvalidTransitionError（rollback 等同步入口非法状态）映射 409 CHANGE_STATE_CONFLICT（通用码层，api-handbook 未单列该业务场景）
- 路由注册顺序 change 先于 ledger（/changes 静态段先于 /:id 进路由树）；既有 4 个测试 RegisterRoutes 调用点追加 newChangeHandlerFixture(t)（独立 fakes，同 newDashboardSettingsHandlers 口径）
- FinishedAt/LatencyMs 为查询期从持久化时间戳推导（HeartbeatAt 终值-ExecutedAt/max 完成时点），非独立持久化字段

## Test Results
- **Tests Executed**: Yes
- **Passed**: 63
- **Failed**: 0
- **Coverage**: 86.9%

## Acceptance Criteria
- [x] GET/POST /changes 列表状态 Tab 筛选；POST 生成清单四阻断映射 409（SCAN_STALE/SAN_INSUFFICIENT/CHANGE_IN_FLIGHT/NEW_CERT_FINGERPRINT_ONLY）；GET /:id 详情含 ChangeList 结构
- [x] POST /:id/confirm（batchConf 越界 400/MaxBatchRatio>0.5 拒绝）、/:id/execute、/:id/confirm-batch（409 BATCH_NOT_CONFIRMABLE）、/:id/cancel（409 CHANGE_NOT_CANCELLABLE）
- [x] GET /:id/progress 逐项状态轮询（itemStates：pending/running/success/failed/rate_limited/rolled_back/skipped + error + 批次）
- [x] POST /:id/rollback 仅成功项；无效目标 409 ROLLBACK_TARGET_INVALID（附转人工提示语义）
- [x] GET /:id/audit 返回 {logs[]{at,actor,action,detail,itemId?}}，action 覆盖 create/confirm/execute/item_result/rollback/verify/orphan_cleanup
- [x] 报告 GET /:id（终态）返回 ChangeReport 全字段（Summary/Items/Verify{ProbePass,ProbeDiff,ProbeSkipped,Unmet}/OrphanCleanup/UnmetDomains/FinishedAt）；响应无任何私钥/密文/凭证字段

## Notes
Hard Rules 遵循：异步执行失败走 progress itemStates status 字段（rollback 项级失败 200+rollback_failed 状态已测）；audit 端点仅 GET 只读。验证：go build ./... 通过；go vet ./internal/cert/... 通过；go test -count=1 ./internal/cert/... 全绿（web 包 63 通过/0 失败，覆盖率 86.9%；cross-pkg 下 change_query_service 函数均 75%+，多数 90%+）。ListPage mongo 集成测试在本机无 Mongo 时按仓内既定行为 SKIP（docker 不可用）。审计写入侧生产接线（internal/audit 适配）与报告存档读写实现归 7.2，本任务交付查询端点契约与 nil 安全回退。未提交 git（按指令）。
