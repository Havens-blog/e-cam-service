---
status: "completed"
started: "2026-08-19 01:22"
completed: "2026-08-19 01:50"
time_spent: "~28m"
---

# Task Record: 5.7 批量执行引擎（任务框架子任务派发、心跳 heartbeatAt、executing-timeout、rate_limited 退避上限→failed、分批 floor(total/2) 门控、ConfirmBatch）

## Summary
批量执行引擎（ChangeExecuteService）：Confirm 快照确认时点重校验（新鲜度+引用一致性）与批次分配一次性固化（字典序稳定排序、首批=min(BatchSize,floor(total/2))、余量均分末批可不足额、MaxBatchRatio 硬校验）；Execute 派发当前批 pending 项（批间暂停 409 BATCH_NOT_CONFIRMABLE）；ExecuteItem 子任务凭持久化 resourceRef 重构 DeployTarget 经通道部署，逐项 panic/失败隔离，30s 心跳，CLOUD_API_RATELIMITED 限流退避（rate_limited 可见→上限耗尽 failed），每项终态后订单按剩余项重算（executing↔verifying 批间循环、abort 收敛 cancelled）；ConfirmBatch 双门控（上一批全部 success + 5.10 BatchVerifyChecker 批级验证达标）→ currentBatch+1 放行；RecoverTimedOutItems 心跳超时恢复（failed(EXEC_TIMEOUT)+通知+重算）。子任务执行器挂 internal/task 框架（taskx.TaskExecutor + Queue 派发器），新增 BatchVerifyChecker/ExecuteAlertNotifier/ChannelCredentialSource 注入端口与 AccountCredentialSource ��产实现；仓储扩展批次固化/领取/终态/超时扫描原子原语（Mongo + certtest fakes）。

## Changes

### Files Created
- internal/cert/service/execute_service.go
- internal/cert/service/execute_service_test.go

### Files Modified
- internal/cert/domain/errors.go
- internal/cert/domain/repository.go
- internal/cert/repository/change_order.go
- internal/cert/repository/change_item.go
- internal/cert/certtest/change_fakes.go
- internal/cert/service/probe_service_test.go

### Key Decisions
- 子任务执行器以 pkg/taskx.TaskExecutor（ChangeItemExecutor）挂 internal/task 框架，ExecuteService 经 SubtaskDispatcher 端口派发（生产 TaskxItemDispatcher→Queue.Submit；测试同步派发器），项级领取 CAS 保证框架重投递幂等
- Hard Rule「单批 ≤ floor(total/2)」对全部批生效：有效批大小 = EffectiveBatchSize（5.3 产物）且后续批同按该值截断；MaxBatchRatio 交叉校验作用于实际生效批大小���Enabled=false 单批全量仅 total≤1 允许
- 批间流转：批执行完成 EnterVerify 原子写 verifying+verifyWindowUntil+batchInfo.paused/pausedAt（pipeline update 保持未分批单 batchInfo 缺失），ConfirmBatch 从 verifying 或 executing+paused 双入口 CAS 放行（AdvanceBatch）
- executing 中止收敛判据：status=skipped 且 Error 为空（Cancel 的 MarkPendingSkipped 不写 error，生成期不可执行项携带 Reason）或后续批 pending 全部消失 → cancelled；终批正常完成进最终验证窗口（终局判定归 5.10/7.1）
- 引擎级限流退避为部署器（5.4/5.5）有界重试之外的二级闸门（次数+总时长双闸，默认 3 次/30s+2m/3m），退避期间 MarkRateLimited 保活心跳防 executing-timeout 误判
- executing-timeout/凭证解析等运维处置通知走独立 ExecuteAlertNotifier 端口（nil=no-op），不计入 PRD 四类业务告警口径（同 scan-timeout 先例）

## Test Results
- **Tests Executed**: Yes
- **Passed**: 63
- **Failed**: 0
- **Coverage**: 79.7%

## Acceptance Criteria
- [x] Confirm：快照重校验（新鲜度/引用一致性）；批次分配一次性固化——字典序排序、首批=min(BatchSize,floor(total/2))、余下按 BatchSize 均分末批可不足额、逐项 batchNo、batchInfo、MaxBatchRatio ≤0.5 硬校验
- [x] Execute：仅派发 batchNo=currentBatch 项；子任务仅凭持久化 resourceRef 重构 DeployTarget；逐项隔离（panic/失败 recover 落项级状态）
- [x] 心跳：30s 间隔更新 heartbeatAt；RecoverTimedOutItems 扫 running 心跳超时 → failed(EXEC_TIMEOUT)+告警+单据重算
- [x] rate_limited：CLOUD_API_RATELIMITED → rate_limited 退避重试；上限耗尽 → failed；进度轮询可见
- [x] ConfirmBatch：上一批全部 success 且批级验证达标（BatchVerifyChecker）→ currentBatch+1；否则 409 BATCH_NOT_CONFIRMABLE；批间暂停 paused/pausedAt
- [x] 每项终态后按剩余项重算（executing↔verifying 循环、终态收敛）；集成测试：批次分配（含 floor 上限）、部分失败隔离、心跳超时恢复、续批门控 409

## Notes
go build/vet 全绿；internal/cert 全部 7 包测试通过（-race 因宿主无 cgo/gcc 不可用）。coverage 79.7% 为 service 包整体（含既有文件），本任务 execute_service.go 函数均值 ~84%。gofmt -l 报 87 个 CRLF 行尾历史文件（全仓既有状态，非本任务引入；新文件为 LF 且 gofmt 干净）；golangci-lint 未安装（Makefile lint 按既有逻辑跳过）。BatchVerifyChecker 本任务仅以接口消费（nil=安全侧不放行），5.10 落地后联调；verifyExpected 刷新与窗口终局判定归 5.10/7.1。
