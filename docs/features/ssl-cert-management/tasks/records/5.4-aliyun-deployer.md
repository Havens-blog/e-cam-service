---
status: "completed"
started: "2026-08-19 00:27"
completed: "2026-08-19 00:41"
time_spent: "~14m"
---

# Task Record: 5.4 阿里云部署器（CDN/DCDN/WAF/LB 两段式绑定+孤儿候选+限流退避上限）

## Summary
实现阿里云 CloudDeployer（AliyunDeployer）：将 3.1 cloudx 阿里云证书五方法适配组装为 deployer 层 CloudDeployer 端口实例，经 5.3 CloudAPIChannel 两段式编排注入。覆盖 cdn/dcdn/waf/alb/nlb 五产品（按 DeployTarget.product 路由）；限流 ErrCloudRateLimited 固定序列退避（1s/2s/4s/8s）+ 次数上限（5）与总时长上限（15s）双闸有界重试，耗尽经 %w 保留哨兵语义供 5.7 映射 rate_limited/failed；应用 poc-notes B2/C2/C7 修正——CAS 上传名 ecam-{指纹前8}-{unix秒}-{随机4hex} 逐次唯一（≤63 截断，重试换名，命名冲突立即换名重试不占退避）；ListReferences 指纹解析口径同 3.5（映射反查→GetCert SHA256 对齐→确定性占位指纹，同公式可对账）；ALB/NLB 监听 {certId}-{region} ID 形态在 BindResource 归一化。端到端经 CloudAPIChannel 验证 DeployResult 三字段与二段失败 CleanupOrphan 补偿+映射 active→orphan。3.1 适配签名零修改（经 aliyunCertAPI 窄接口消费）。

## Changes

### Files Created
- internal/cert/deployer/aliyun_deployer.go
- internal/cert/deployer/aliyun_deployer_test.go

### Files Modified
无

### Key Decisions
- CloudDeployer.UploadCert 无 name/product 参数（5.3 端口签名权威）：第一段统一以 CDN 口径上传取 CAS 纯数字 ID，ALB/NLB 监听引用的 {certId}-{region} 形态由 BindResource 按产品归一化追加（已带后缀幂等保持），不改 3.1 签名
- CAS 命名冲突（B2）按 ServerError 错误码/文案保守启发式判定（实际码待实网确认 poc-notes F9⏳），冲突重试立即换名不占退避睡眠，与限流共用 MaxAttempts 重试预算
- 退避参数缺省保守值（5 次/15s/1-2-4-8s 固定序列），AlertConfig.thresholds 现无退避字段（已核实 4.2 域），提供 WithAliyunRetryPolicy 选项供装配层从应用 config 注入
- ListReferences 指纹解析复用 3.5 同公式占位指纹（sha256(certscan-unresolved:cloud|account|certId)），保证扫描路径与通道发现路径结果可对账；mappings 可空（nil 跳过映射反查直查 GetCert）

## Test Results
- **Tests Executed**: Yes
- **Passed**: 22
- **Failed**: 0
- **Coverage**: 91.1%

## Acceptance Criteria
- [x] AliyunDeployer 实现 CloudDeployer 供 5.3 CloudAPIChannel 注入，覆盖 cdn/dcdn/waf/alb/nlb 五产品，按 DeployTarget.product 路由到 3.1 适配方法
- [x] Deploy 返回 DeployResult{NewCloudCertID, OldCloudCertID, OrphanCandidate}；OldCloudCertID 从引用快照读取（执行前）
- [x] 限流 ErrCloudRateLimited 退避重试固定序列有界上限，耗尽显式失败不无限重试（rate_limited 状态由 5.7 承接）
- [x] 第二段绑定失败 → CleanupOrphan 清理未绑定云侧证书 + OrphanCandidate=true（映射 active→orphan 供 5.9）
- [x] mock SDK 测试：两段式成功、第二段失败→补偿清理、限流退避至耗尽、五产品路由
- [x] go build ./internal/cert/... 全绿；不修改 3.1 适配签名

## Notes
验证：go build ./internal/cert/... 全绿；go test ./internal/cert/... 全绿（deployer 包 49 测试，其中本任务新增 22）；go vet PASS；gofmt -s 已格式化；golangci-lint 未安装（Makefile lint 门按项目定义跳过并告警）。覆盖率 91.1%（deployer 包，go test -cover）。B2 修正项已应用并通过测试；poc-notes F9 命名冲突实际错误码⏳实网确认，启发式判定保守处理。ALB/NLB 绑定生效语义与 CAS 名称规则为桌面验证（mock+文档），实网验证点随 5.12 PoC 遗留项。
