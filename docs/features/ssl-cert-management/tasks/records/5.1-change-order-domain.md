---
status: "completed"
started: "2026-08-18 22:59"
completed: "2026-08-18 23:27"
time_spent: "~28m"
---

# Task Record: 5.1 变更单领域与状态机（9 态、activeMutex 部分唯一索引互斥、草稿/取消/CHANGE_NOT_CANCELLABLE、保护期字段）

## Summary
变更单领域状态机与互斥层落地：domain.Transition 9 态合法迁移白名单 + CancelModeOf 取消三分支分类（整单/中止/不可取消）；ChangeService 骨架（Transition/Cancel/CancelByTimeout）路由至仓储原子原语——活跃态 TransitionActive 写 activeMutex=oldCertFingerprint（update 路径补充 dup-key→ErrChangeInFlight 映射，check-then-update 竞态窗口由 uk_active_mutex 索引关闭）、完成类终态 TransitionTerminalWithProtect 同一原子 update 固化 protectUntil 并 $unset token、其余终态 TransitionTerminal $unset token；Cancel 整单路径未执行项标 skipped 后转 cancelled；CancelByTimeout 按 pauseTimeoutHours 扫描批间暂停超���单（executing 限定防终态单重复扫描）；旧证书保护期经 CertificateRepository.SetProtectUntil 固化（2.3 删除拦截依据，只延长不缩短）。

## Changes

### Files Created
- internal/cert/domain/change_order_state.go
- internal/cert/domain/change_order_state_test.go
- internal/cert/service/change_service.go
- internal/cert/service/change_service_test.go
- internal/cert/certtest/change_fakes.go

### Files Modified
- internal/cert/domain/errors.go
- internal/cert/domain/repository.go
- internal/cert/repository/change_order.go
- internal/cert/repository/change_item.go
- internal/cert/repository/certificate.go
- internal/cert/repository/change_order_test.go
- internal/cert/repository/certificate_test.go
- internal/cert/service/probe_service_test.go
- internal/cert/certtest/certtest.go

### Key Decisions
- 迁移白名单严格按任务 AC：rolled_back/rollback_failed 仅自 completed/partial_completed（PRD 回滚入口在 executing/verifying 指发起时机，终态迁移自完成类终态）；verifying→cancelled 非法（不可取消走 Cancel 的 CHANGE_NOT_CANCELLABLE）
- Phase 1 的 ActiveChangeStatuses（含 draft 的 4 态划分）保留不动：draft 从不作为迁移目标，Transition 按 IsTerminalChangeStatus(target) 路由，token 语义与 AC-2 的 3 活跃态完全一致
- TransitionActive 补充 mapDupKey：unique 索引对 update 同样强制，draft→pending_confirm 在途冲突在索引层被拒并映射 ErrChangeInFlight（AC-5：应用层预检查仅为快速失败，正确性归索引）
- CancelByTimeout 的 ListPausedBefore 增加 status=executing 限定：已取消单残留 batchInfo.paused 标记不重复进入扫描集（幂等性由测试锁定）
- 整单取消先标 skipped 再迁终态（标记幂等、两步间崩溃可重试收敛）；终态迁移不做状态 CAS，以终态覆盖终态+幂等 $unset 保证无 token 残留（注释声明）
- 旧证书保护期（Certificate.protectUntil，2.3 拦截字段）与订单 protectUntil 同值固化，订单终态先行、证书保护随后（幂等、失败上抛不回滚终态）

## Test Results
- **Tests Executed**: Yes
- **Passed**: 61
- **Failed**: 0
- **Coverage**: 100.0%

## Acceptance Criteria
- [x] Transition(current, target) 9 态迁移白名单（draft→pending_confirm→executing↔verifying→completed/partial_completed→rolled_back/rollback_failed；cancelled 来源 draft/pending_confirm/executing），非法迁移报错
- [x] activeMutex：进活跃态写 token=oldCertFingerprint，终态同一原子 update $unset，冲突映射 ErrChangeInFlight（CHANGE_IN_FLIGHT）
- [x] Cancel：draft/pending_confirm/批间暂停整单取消（未执行项 skipped+token 原子清除）；executing 中止（pending 标 skipped、running 不中断）；verifying/终态 409 CHANGE_NOT_CANCELLABLE
- [x] protectUntil：进 completed/partial_completed 按 rollbackProtectDays 设定（订单原子固化+旧证书保护期，2.3 已拦截该字段）
- [x] 互斥正确性由 DB 索引保证（insert 与 update 路径均索引强制，应用层不做正确性预检查）
- [x] 领域测试：迁移矩阵、并发双 insert 仅一张活跃（mongox 实例 duplicate key 验证）、Cancel 各分支、CHANGE_NOT_CANCELLABLE

## Notes
验证：go build ./... 通过；go test ./internal/cert/... 全绿（无 DSN 时集成测试按设计 skip）；集成路径另经 CERT_TEST_MONGODB_DSN 对真实 MongoDB（uk_active_mutex 部分唯一索引）实测通过——含并发双 insert 恰一张活跃、update 路径 token 冲突→ErrChangeInFlight、完成类终态 protectUntil+token 同原子、暂停超时扫描、pending 标 skipped、保护期只延长不缩短；远端实例无测试库残留（ecam_cert_test_* 为 0）。新增文件覆盖率 100%（150/150 语句，go tool cover 实测；domain 包 95.3%、service 包 77.3% 聚合值受既有文件稀释）。已知环境事项：(1) gofmt -l 因全仓 CRLF 检出树级现象（git autocrlf 提交时归一，非本任务引入）；(2) golangci-lint 未安装，make lint 自跳过；(3) 远端实例上两个既有集成测试（TestCertificateRepo_ListPage 指纹夹具循环子串缺陷、TestCertificateRepo_GetByIDAndAttachPrivateKey 读己之写延迟）失败，路径不在本任务改动内。执行中一次 git stash 事故已完全恢复（工作树与提交前状态一致，stash 已删除）。
