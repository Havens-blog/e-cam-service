---
id: "7.summary"
title: "Phase 7 Summary"
priority: "P0"
estimated_time: "15min"
dependencies: ["7.1", "7.2"]
type: "doc.summary"
mainSession: false
---

# 7.summary: Phase 7 Summary

## Description

Generate a structured summary of all completed tasks in phase 7. This summary is read by subsequent phase tasks to maintain cross-phase consistency.

## Instructions

### Step 1: Read all phase task records

Read each record file from `docs/features/ssl-cert-management/tasks/records/` whose filename starts with `7.` and does NOT contain `.summary` (e.g., `7.1-*.md`, `7.2-*.md`). Exclude the phase summary's own prior record if one exists.

### Step 2: Extract structured data into the summary field

The `summary` field in `record.json` MUST follow this exact template. Copy it verbatim and fill in the values.

## Reference Files

- All phase task records: `docs/features/ssl-cert-management/tasks/records/7.*.md`
- Design reference: `docs/features/ssl-cert-management/design/tech-design.md`

## Acceptance Criteria

- [ ] All phase task records have been read
- [ ] Summary follows the exact 5-section template
- [ ] Types & Interfaces Changed table is populated
- [ ] Record created via `/submit-task`

## Hard Rules

- MUST NOT write new feature code — this is documentation only
